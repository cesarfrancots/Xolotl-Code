---
phase: 04-w6-combat-diplomacy
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - tauri-app/src-tauri/src/civilization.rs
autonomous: true
requirements: [WAR-04]
must_haves:
  truths:
    - "When a predator_incursion forecast FIRES in tick_environment, spawn_predators adds net-new wild predator entities (kind=='predator', civ_id==None) near the threatened colony, with deterministic format! ids (no uuid/clock) (WAR-04)"
    - "step_predators moves each predator toward the nearest living colony and, when in range, removes living axolotl entities of that civ (bounded), reduced by that civ's civ_strength defense — population mirror reflects the losses after resolve_environment (WAR-04)"
    - "A strong civ (high civ_strength) culls predators (removes predator entities) / takes less damage than a weak civ; killed predators drop a food resource gain (WAR-04)"
    - "Predators expire by lifespan (age >= lifespan) and are culled; the predator pass is byte-deterministic for a fixed (seed, turn) using a predator-specific salt (WAR-04)"
    - "The predator world pass runs in advance_civ_turn after the decision loop and after combat but BEFORE resolve_environment, so predator casualties land before the population mirror re-syncs (WAR-04)"
  artifacts:
    - path: "tauri-app/src-tauri/src/civilization.rs"
      provides: "spawn_predators + step_predators helpers, the predator_incursion spawn hook in tick_environment, the predator world pass call in advance_civ_turn, and WAR-04 spawn/hunt/defend/expire/determinism tests"
      contains: "fn step_predators"
  key_links:
    - from: "tick_environment predator_incursion fire branch"
      to: "spawn_predators"
      via: "spawn call added in the forecast-fires path (keeps the existing quarrel_pressure modifier)"
      pattern: "spawn_predators\\("
    - from: "advance_civ_turn"
      to: "step_predators"
      via: "predator world pass between combat and resolve_environment"
      pattern: "step_predators\\("
    - from: "step_predators damage"
      to: "civ_strength"
      via: "defense reduces predator damage; strong civs cull predators"
      pattern: "civ_strength\\("
---

<objective>
Implement WAR-04 wild predators: spawn net-new predator entities when Phase 3's `predator_incursion` forecast fires, and a deterministic predator world pass that moves predators toward the nearest colony, attacks (removing axolotl entities, bounded), lets civs defend with `civ_strength` (strong civs cull predators and take less damage; culled predators drop food), and expires predators by lifespan.

Purpose: This is the only genuinely net-new concept in Phase 4 — there are NO wild fauna today; `predator_incursion` currently only pushes a morale modifier. Predators reuse the wild-entity slot (`CivEntity{civ_id:None}`), the `civ_strength` seam from 04-02 (defense), and the same entity-removal casualty discipline + before-resolve_environment placement as combat (because population is a mirror). Spawn is hooked into `tick_environment` (turn start); the hunt/defend pass runs in the same post-decision window as combat.

Output: Updated `civilization.rs` with `spawn_predators`/`step_predators`, the tick_environment spawn hook, the predator pass in advance_civ_turn, and WAR-04 unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-w6-combat-diplomacy/04-CONTEXT.md
@.planning/phases/04-w6-combat-diplomacy/04-RESEARCH.md
@.planning/phases/04-w6-combat-diplomacy/04-PATTERNS.md
@.planning/phases/04-w6-combat-diplomacy/04-VALIDATION.md
@.planning/phases/04-w6-combat-diplomacy/04-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified against civilization.rs 2026-06-07. Use directly; no exploration needed. -->

CivEntity (civilization.rs:348-400) — #[derive(Default)], so `..Default::default()` is valid. A predator is:
```rust
CivEntity {
    id: format!("predator-{turn}-{n}"),   // deterministic, no uuid/clock (mirror disaster ids :5154)
    kind: "predator".to_string(),
    role: "predator".to_string(),
    name: "Wild predator".to_string(),
    x, y,
    health: 1.0,
    civ_id: None,                          // wild slot (:358-361)
    stage: "adult".to_string(),
    age: 0,                                // doubles as lifespan counter
    ..Default::default()
}
```
civ_id:None entities are wild fauna; the renderer funnels them through createAxo (untinted) — EXPECTED, do NOT add a renderer change (RESEARCH Pitfall 5).

tick_environment predator_incursion fire branch (civilization.rs:5161-5208) — the forecast FIRES inside `if forecast.remaining_turns == 0 {` (after apply_disaster_to_tiles :5168). The modifier match is at :5174-5180; `"predator_incursion" => Some("quarrel_pressure")` (:5178). KEEP that modifier (RESEARCH Open Q3) AND add the spawn in the same fire path. The fired forecast carries `epicenter_x: u32` (:519), `radius`, `intensity`. disaster_duration("predator_incursion") == 3 (:5134-5143) — tie lifespan to it (e.g. 4-6 turns; Claude's discretion).

advance_civ_turn insertion window (civilization.rs:805-888) — decision loop ends :871, resolve_combat (04-02) inserted there, resolve_environment loop starts :874. step_predators(&mut snapshot) goes AFTER resolve_combat and BEFORE the resolve_environment loop.

Helpers: colony_center(snapshot, civ_id) -> (u32,u32) (:1673), dist2(ax,ay,bx,by) -> u64 (:1691) for nearest-colony, civ_index (:1564), civ_entities (:1569), civ_strength (added in 04-02 — defense), round1 (:4919), rand_range/rand_f/next_rng (:4717/4713/4704), push_log (:4455), consume (:4051), civ_label (:1585).

Entity-removal retain idiom (elder-death :2649): `snapshot.world.entities.retain(|e| !victims.contains(&e.id))`. Population is a MIRROR (:2767-2773, filters kind=="axolotl"&&stage!="egg") — predator casualties MUST remove axolotl entities AND run before resolve_environment.

Per-pass RNG salt: use a DISTINCT predator salt (e.g. 0xBADD_CA75), different from combat's 0xC0FF_EE01 and civ_turn_order's 0x51ED_2701, so passes stay uncorrelated:
```rust
let mut rng = (snapshot.seed ^ snapshot.turn.wrapping_mul(0x9E37_79B9) ^ 0xBADD_CA75).max(1);
```

Test fixtures (mod tests :5282): multi_civ_snapshot(seed,n) (:6485) — civ-1 has axolotl entities; push predator entities directly into s.world.entities for step_predators tests. Determinism pattern (clone, run on both, assert serde_json::to_string equal) at :7101. tick_environment_forecast_then_fire (:6935) shows how to drive a forecast to fire for the spawn test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: spawn_predators + tick_environment predator_incursion spawn hook (WAR-04)</name>
  <files>tauri-app/src-tauri/src/civilization.rs</files>
  <read_first>
    - tauri-app/src-tauri/src/civilization.rs: CivEntity (348-400), tick_environment fire branch (5161-5208, predator_incursion modifier 5178), CivDisaster (516-523), disaster_duration (5134-5143), colony_center (1673), dist2 (1691), civ_id_for (1580), the disaster id format (5183-5186), entity push (seed_founders 1535 / extend 2756)
    - .planning/phases/04-w6-combat-diplomacy/04-PATTERNS.md: "spawn_predators helper" + "Predator spawn hook in tick_environment" sections
    - .planning/phases/04-w6-combat-diplomacy/04-RESEARCH.md: Pattern 4 (predator lifecycle), Open Q3 (keep quarrel_pressure modifier), Pitfall 2 (determinism), Pitfall 5 (renderer — do NOT change)
    - .planning/phases/04-w6-combat-diplomacy/04-02-SUMMARY.md (the combat pass placement + salt convention this plan mirrors)
  </read_first>
  <behavior>
    - Test (predator_incursion_spawns_predators): drive a forecast of kind "predator_incursion" to fire in tick_environment (mirror tick_environment_forecast_then_fire :6935); after the tick, snapshot.world.entities contains >=1 entity with kind=="predator" && civ_id==None, and their ids match the deterministic `predator-{turn}-{n}` pattern.
    - Test: the existing quarrel_pressure modifier is STILL pushed when predator_incursion fires (Open Q3 — predators + morale pressure coexist; no Phase-3 test regresses).
    - Test (determinism): firing predator_incursion at the same (seed, turn) on two clones produces byte-identical world.entities (predator ids/positions identical).
    - Test: a non-predator disaster (e.g. drought) spawns NO predator entities.
  </behavior>
  <action>
    Per RESEARCH Pattern 4 + Open Q3. spawn_predators is net-new entity creation; ids are deterministic (no uuid/clock). Place spawn_predators near other entity/leaf helpers (e.g. after colony_center or near the env helpers).

    1. `spawn_predators(snapshot, near_x, count, rng)` — push `count` predator entities near the threatened colony. Find the nearest living colony center to the disaster epicenter (iterate living civs, `colony_center`, pick min `dist2` to `near_x`), then place predators within a small offset of that center. Deterministic positions from `rng` (rand_range for small x/y jitter, clamped to world bounds). Use the `predator-{turn}-{n}` id format. Set `civ_id: None`, `kind/role: "predator"`, `age: 0`, `..Default::default()`:
    ```rust
    fn spawn_predators(snapshot: &mut CivSessionSnapshot, epicenter_x: u32, count: u32, rng: &mut u32) {
        // nearest living colony center to the epicenter
        let target = snapshot.civs.iter().filter(|c| c.alive)
            .map(|c| colony_center(snapshot, &c.id))
            .min_by_key(|(cx, _)| (i64::from(*cx) - i64::from(epicenter_x)).unsigned_abs());
        let Some((cx, cy)) = target else { return; }; // no living civ → nothing to hunt
        let turn = snapshot.turn;
        let width = snapshot.world.width;
        let mut spawned = Vec::new();
        for n in 0..count {
            let jitter_x = rand_range(rng, -3.0, 3.0) as i64;
            let px = (i64::from(cx) + jitter_x).clamp(0, i64::from(width) - 1) as u32;
            spawned.push(CivEntity {
                id: format!("predator-{turn}-{n}"),
                kind: "predator".to_string(),
                role: "predator".to_string(),
                name: "Wild predator".to_string(),
                x: px, y: cy, health: 1.0,
                civ_id: None, stage: "adult".to_string(), age: 0,
                ..Default::default()
            });
        }
        snapshot.world.entities.extend(spawned);
    }
    ```
    (Adjust count: Claude's discretion, e.g. 1-3 scaled by forecast.intensity.)

    2. Hook into `tick_environment` (civilization.rs:5161-5208), inside the `if forecast.remaining_turns == 0 {` fire path, AFTER `apply_disaster_to_tiles` (:5168) and AFTER the existing modifier push (keep it — Open Q3). Add a predator-specific rng + spawn call ONLY for predator_incursion:
    ```rust
        if forecast.kind == "predator_incursion" {
            let mut prng = (snapshot.seed ^ snapshot.turn.wrapping_mul(0x9E37_79B9) ^ 0xBADD_CA75).max(1);
            let count = 1 + (forecast.intensity.clamp(0.0, 2.0) as u32); // 1-3 predators
            spawn_predators(&mut snapshot, forecast.epicenter_x, count, &mut prng);
        }
    ```
    Place this so it does NOT perturb the existing forecast/modifier/log/disaster-push order (the tick is documented byte-deterministic :5153-5155; adding a self-contained spawn with its own salt preserves that). Run `cargo fmt`.

    3. Clippy: `as u32`/`as i64`/`as u32` casts on jitter — use the codebase style; `unsigned_abs` for the abs-diff; ZERO new warnings vs the 16 baseline.
  </action>
  <verify>
    <automated>cd tauri-app/src-tauri && cargo fmt --all -- --check 2>&1 | tail -3 && cargo test --no-run 2>&1 | tail -5 && cargo clippy --all-features -- -D warnings 2>&1 | grep -c "^error" </automated>
  </verify>
  <acceptance_criteria>
    - civilization.rs contains `fn spawn_predators`
    - tick_environment contains `spawn_predators(` inside the predator_incursion fire path AND still contains `"predator_incursion" => Some("quarrel_pressure")` (modifier kept, Open Q3)
    - predator entity creation uses `format!("predator-{turn}-{n}")` (deterministic id) and `civ_id: None`
    - spawn rng uses the predator salt `0xBADD_CA75`
    - `cargo test --no-run` exits 0
    - `cargo clippy --all-features -- -D warnings` error count == 16 (ZERO new)
    - `cargo fmt --all -- --check` exits 0
    - New tests present: predator_incursion_spawns_predators (+ deterministic + modifier-still-pushed + non-predator-disaster-spawns-none assertions)
  </acceptance_criteria>
  <done>When predator_incursion fires, net-new wild predator entities (civ_id=None, deterministic ids) spawn near the threatened colony while the existing quarrel_pressure modifier still fires; spawning is byte-deterministic and adds zero clippy warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: step_predators (hunt + civ_strength defense + cull + food drop + expire) + predator world pass in advance_civ_turn (WAR-04)</name>
  <files>tauri-app/src-tauri/src/civilization.rs</files>
  <read_first>
    - tauri-app/src-tauri/src/civilization.rs: elder-death retain (2636-2650), population mirror (2767-2773), colony_center (1673), dist2 (1691), civ_strength (added 04-02), civ_index (1564), civ_entities (1569), consume (4051), push_log (4455), advance_civ_turn insertion window (871-874, resolve_combat call from 04-02)
    - .planning/phases/04-w6-combat-diplomacy/04-PATTERNS.md: "step_predators helper" + "Combat world pass" (placement) sections + Shared Patterns (entity-removal casualties, RNG salt)
    - .planning/phases/04-w6-combat-diplomacy/04-RESEARCH.md: Pattern 4 (hunt/expire), Pitfall 1 (mirror), Pitfall 4 (bounded), Pitfall 2 (determinism)
    - .planning/phases/04-w6-combat-diplomacy/04-02-SUMMARY.md (kill_axolotls / entity-removal helper if it can be reused for predator casualties)
  </read_first>
  <behavior>
    - Test (step_predators_hunt_and_expire): a snapshot with civ-1 axolotls + predators near the colony — after step_predators civ-1 has FEWER living axolotl entities (hunted), and after a following resolve_environment civ-1's population mirror reflects the loss. Predators with age >= lifespan are removed; surviving predators' age incremented.
    - Test (strength_defends_against_predators): a strong civ (high civ_strength via pop/tools/tech/territory) loses fewer axolotls AND culls more predators than a weak civ facing the same predator count (assert the strong civ has more survivors and/or fewer predators remain).
    - Test: a culled predator drops food — when step_predators removes a predator via defense, the defending civ's "food" resource increases (bounded, conserved-style gain).
    - Test (determinism): step_predators on two clones at the same (seed, turn) yields byte-identical world.entities + civ resources.
    - Test: step_predators never reduces a civ to 0 living axolotls in one step (bounded, leave >=1 — same no-wipeout discipline as combat).
  </behavior>
  <action>
    Per RESEARCH Pattern 4 + the entity-removal discipline. step_predators reuses civ_strength (04-02) for defense and the same bounded/entity-removal/before-resolve_environment rules as combat.

    1. `step_predators(snapshot: &mut CivSessionSnapshot)` — one deterministic pass. Seed `rng = (snapshot.seed ^ snapshot.turn.wrapping_mul(0x9E37_79B9) ^ 0xBADD_CA75).max(1)`. Steps (process predators in stable id-sorted order for determinism):
       a. For each predator (kind=="predator"), find the nearest living colony (min dist2 over living civs' colony_center). Move the predator one step toward that center (clamp to world bounds) — mutate x/y.
       b. If the predator is in range (e.g. dist2 <= a small threshold), it attacks that civ: base `damage` (e.g. 1) reduced by the civ's `civ_strength` (defense) — strong civs take 0 damage. Accumulate per-civ axolotl kills; remove that many living axolotl entities of the civ via the entity-removal retain (reuse `kill_axolotls` from 04-02 if exported in-module, else inline the sorted-victims retain). BOUNDED: never remove the civ's last living axolotl (leave >=1).
       c. Defense cull: with probability/threshold scaled by `civ_strength` (strong civ → high cull chance), the predator is killed. Collect culled predator ids. For each culled predator, credit the defending civ's "food" resource (`+= FOOD_DROP`, e.g. 3) — the "killed predators drop food" rule.
       d. Expire: increment each surviving predator's `age`; mark predators with `age >= LIFESPAN` (e.g. 5, tied to disaster_duration 3 + slack) for removal.
       e. Remove all culled + expired predator ids in one `entities.retain(|e| !dead.contains(&e.id))`. Log a summary via push_log if any predator hunted or was culled.
       Mind borrow rules: collect all kills/culls/food-credits into local Vecs first, then apply mutations (retain + resource credits) after the read phase, to avoid aliasing snapshot.world.entities while iterating.

    2. Wire the predator pass into `advance_civ_turn`: AFTER the `resolve_combat(&mut snapshot, &mut attacks);` call (04-02) and BEFORE the `resolve_environment` loop (:874):
    ```rust
        step_predators(&mut snapshot);
    ```
    So order is: decision loop → resolve_combat → step_predators → resolve_environment. Both casualty passes land before the population mirror re-syncs.

    3. Run `cargo fmt`. Clippy: `as f32`/`as u32` casts (use `f64::from` where it helps), `too_many_lines` on step_predators → factor the cull/hunt into small helpers if needed; ZERO new vs the 16 baseline.
  </action>
  <verify>
    <automated>cd tauri-app/src-tauri && cargo fmt --all -- --check 2>&1 | tail -3 && cargo test --no-run 2>&1 | tail -5 && cargo clippy --all-features -- -D warnings 2>&1 | grep -c "^error" </automated>
  </verify>
  <acceptance_criteria>
    - civilization.rs contains `fn step_predators`
    - step_predators uses `civ_strength(` (defense) and `.retain(` (entity removal for both axolotl casualties and predator culls) — NO `population -=` decrement
    - step_predators credits "food" on a cull (contains a `"food"` resource credit) and increments predator `age` for expiry
    - advance_civ_turn contains `step_predators(&mut snapshot)` appearing AFTER `resolve_combat(` and BEFORE the `resolve_environment(&mut snapshot, civ_id)` loop in source order
    - step_predators seeds rng with the predator salt `0xBADD_CA75`
    - `cargo test --no-run` exits 0
    - `cargo clippy --all-features -- -D warnings` error count == 16 (ZERO new)
    - `cargo fmt --all -- --check` exits 0
    - New tests present: step_predators_hunt_and_expire, strength_defends_against_predators, a food-drop assertion, a determinism assertion, and a no-wipeout assertion
  </acceptance_criteria>
  <done>Predators hunt the nearest colony (removing axolotl entities, bounded, before the mirror re-syncs), strong civs defend with civ_strength (fewer losses + more culls), culled predators drop food, and predators expire by lifespan; the pass is byte-deterministic with its own salt and runs after combat, before resolve_environment; clippy at baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment forecast → predator spawn | A fired `predator_incursion` (derived from seed/turn) creates net-new entities that then mutate civ populations. |
| Predator pass RNG → eval replay | Predator spawn + hunt outcomes must be byte-identical on replay of a (seed, turn). |
| Predator damage → world-state integrity | Unbounded hunting could delete a civ; food drops could distort resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Repudiation (eval integrity) | predator spawn/hunt RNG + entity ids | mitigate | Predator-specific salt `0xBADD_CA75`; ids `format!("predator-{turn}-{n}")`; predators processed in stable id-sorted order; no uuid/clock/HashMap iteration (Tasks 1-2; spawn + step determinism tests). |
| T-04-02 | DoS / availability (game-state) | step_predators / spawn casualties | mitigate | Hunt damage is bounded and never removes a civ's last living axolotl (leave ≥1, same discipline as combat); spawn count capped (1-3). Collapse only via attrition + should_collapse (Task 2 no-wipeout test). |
| T-04-09 | Tampering (state corruption) | population counter vs entity desync | mitigate | Predator casualties remove axolotl ENTITIES via retain and run BEFORE resolve_environment so the mirror re-syncs; no `population -=` (Task 2 mirror-reflects-loss assertion). |
| T-04-11 | Tampering (economic) | predator food drop creating runaway/negative resources | mitigate | Food drop is a small fixed credit only when a predator is actually removed (bounded by predator count); a credit (`+=`) cannot go negative (Task 2 food-drop test). |
| T-04-12 | Availability (back-compat) | old saves with no predator entities / new `predator` kind on load | accept (existing mitigation) | CivEntity is `#[serde(default)]` throughout + `#[derive(Default)]`; a new `kind` value is just a string — old saves load (no predators), new saves carry them. Renderer funnels `civ_id:None` through createAxo (RESEARCH Pitfall 5 — expected, no renderer change). |
</threat_model>

<verification>
- `cargo test --no-run` compiles all predator tests on Windows (exit 0); full `cargo test` runs the WAR-04 spawn/hunt/defend/expire/determinism tests on CI (Linux/macOS) — backend tests cannot run on Windows (gotcha #5).
- `cargo clippy --all-features -- -D warnings` error count stays at the 16-error baseline — ZERO new warnings in predator code.
- `cargo fmt --all -- --check` exits 0.
- Determinism: spawn + step_predators determinism tests assert byte-identical world.entities + resources across clones at a fixed (seed, turn).
- Invariants: no-wipeout, food-drop bounded, population-mirror-reflects-predator-losses green on CI.
- No bindings change in this plan (no CivDecisionAction change) — no regen needed.
- Renderer is unchanged: predators render via the existing createAxo funnel (RESEARCH Pitfall 5) — do not add a renderer change.
</verification>

<success_criteria>
- A fired predator_incursion spawns net-new wild predators near the threatened colony (deterministic ids, civ_id=None), while the existing morale modifier still fires (WAR-04).
- Predators hunt the nearest colony (removing axolotl entities, bounded, before the mirror re-syncs); strong civs defend via civ_strength (fewer losses, more culls); culled predators drop food; predators expire by lifespan (WAR-04).
- The predator pass is byte-deterministic (own salt) and runs after combat and before resolve_environment.
- clippy(baseline) + fmt + test --no-run all clean; no renderer/bindings change.
</success_criteria>

<output>
After completion, create `.planning/phases/04-w6-combat-diplomacy/04-03-SUMMARY.md`
</output>
</content>
