---
phase: 01-w9-lite-multi-model-world-creation-leaderboard
plan: 04
type: execute
wave: 3
depends_on: [2, 3]
files_modified:
  - tauri-app/src/components/civilization/CivilizationView.tsx
autonomous: true
requirements: [CIV-02, CIV-03, ARENA-02, ARENA-03]
must_haves:
  truths:
    - "A persistent top-bar leaderboard above the canvas ranks living civs by score.total and greys collapsed (dead) civs at the bottom"
    - "Clicking a leaderboard row sets selectedCivId, which drives the observer panel and per-civ log filter"
    - "The combined chronological log shows action + rationale per entry, with model reasoning collapsed behind a per-entry expand toggle"
    - "civPilotControls.start accepts an optional civId (additive) and the controlled civ carries a controller tag shown on the leaderboard, set via set_civ_controller"
  artifacts:
    - path: "tauri-app/src/components/civilization/CivilizationView.tsx"
      provides: "Leaderboard top-bar, selectedCivId-driven observer/log, reasoning expand toggle, civPilotControls civId + controller wiring"
      contains: "selectedCivId"
  key_links:
    - from: "leaderboard row onClick"
      to: "setSelectedCivId"
      via: "civStore selection (Plan 02)"
      pattern: "setSelectedCivId"
    - from: "civPilotControls.start({ civId, controller })"
      to: "commands.setCivController"
      via: "set_civ_controller (Plan 01)"
      pattern: "setCivController"
---

<objective>
Surface the multi-civ competition in the UI (CIV-02, CIV-03) and finish the harness driving/attribution wiring (ARENA-02, ARENA-03). Add a persistent top-bar leaderboard (ranked living civs, greyed collapsed civs, row-click selects the civ), rewire the observer panel + per-civ log filter to `selectedCivId`, add the per-entry reasoning expand toggle (D-12 Option B reads `entry.reasoning`), and extend `civPilotControls.start` with an additive `civId` plus a controller tag set via `set_civ_controller`.

Purpose: Turns the now-creatable multi-civ world (Plans 01-02) into a watchable, navigable competition and a harness scoreboard, consuming the persisted reasoning/controller fields from Plan 01.

Output: Updated `CivilizationView.tsx`; new vitest specs for the leaderboard, log filter, and reasoning toggle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-RESEARCH.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-CONTEXT.md
@CLAUDE.md
@tauri-app/src/bindings.ts

<interfaces>
<!-- Verified anchors 2026-06-06. selectedCivId + controller default come from Plan 02; controller/reasoning fields + set_civ_controller from Plan 01. -->

CivilizationView canvas branch (797-803) — leaderboard top-bar mounts as a sibling above the canvas. HUD vocabulary: civ-glass / oklch (806-810).
activeCiv (256): `const activeCiv = snapshot ? primaryCiv(snapshot) : null;` — primaryCiv => civs[0] (civStore.ts:111-113). MUST rewire to selectedCivId.
recentLog (255): `[...snapshot.log].reverse().slice(0, 12)`, rendered by LogPanel (~1904).
Observer consumers: ScorePanel (1034), ResourcesPanel (1043), ColonyPanel/RegionsPanel (1037/1040). Per-civ entity filter idiom: `entity.civ_id === activeCiv.id` (266-273).
civPilotControls type (67-70) + impl (409-444): `start(options?: { goal?, possessId?, requesterId?, continueAfterTask? }): void; stop(): void`.
Event listener (229-245): only acts on payload.snapshot / payload.error; CivEventPayload (148-152).

leaderboard() backend payload fields per civ: id, name, model, color, alive, population, era, score{survival,ethics,intelligence,total}, controller (Plan 01). snapshot.civs is normalized (civStore.ts:81-135) for v1 + v2.
CivLogEntry now has civ_id + reasoning (Plan 01 Option B).
commands.setCivController(id, civId, controller) — new binding from Plan 01.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Leaderboard top-bar + selectedCivId-driven observer/log + reasoning toggle (CIV-02, CIV-03)</name>
  <files>tauri-app/src/components/civilization/CivilizationView.tsx</files>
  <read_first>
    - tauri-app/src/components/civilization/CivilizationView.tsx (canvas branch 797-803, HUD 806-810, activeCiv/recentLog 255-256, observer panels 1034/1043/1037/1040, LogPanel ~1904, entity filter 266-273)
    - tauri-app/src/stores/civStore.ts (selectedCivId/setSelectedCivId from Plan 02, normalizeCiv controller default)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md (Leaderboard top-bar + activeCiv rewire + per-civ log filter sections, Pitfalls 4 & 5)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-RESEARCH.md (sections 3 + 4)
  </read_first>
  <behavior>
    - Test: top-bar renders civs ranked by score.total desc; an alive===false civ is greyed and sorted to the bottom with a "collapsed" marker (D-08).
    - Test: clicking a row calls setSelectedCivId with that civ id (D-09).
    - Test: when selectedCivId is set, the observer panel shows that civ (not civs[0]) and the log filters to entries with civ_id === selectedCivId; when null, log shows all (D-10).
    - Test: a log entry with reasoning renders an expand toggle; rationale always visible, reasoning hidden until expanded (D-11/D-12).
  </behavior>
  <action>
    Per D-06 (persistent top bar), D-07 (minimal rows: swatch/rank/name/score.total), D-08 (dead greyed at bottom), D-09 (row-click -> selectedCivId), D-10 (combined log, filter-on-select), D-11 (action + rationale), D-12 (reasoning collapsed behind toggle, Option B reads entry.reasoning).

    1. Leaderboard top-bar: new sibling above the canvas branch (~797-803). Read `useCivStore(s => s.activeSnapshot)`; derive rows by sorting `snapshot.civs` by `score.total` desc (mirrors backend leaderboard(), RESEARCH 3a — derive from snapshot, NOT the event field). Render living civs first, then `alive === false` civs greyed at the bottom with a "collapsed" label (`alive === false` IS the collapsed state — civilization.rs:441/764; no separate field). Each row = color swatch · rank · name · `score.total` · a small controller badge when `civ.controller` is set. `onClick` -> `setSelectedCivId(civ.id)` (D-09). Style with the existing civ-glass / oklch HUD vocabulary (806-810); collapse gracefully on narrow widths (discretion).
    2. Observer rewire (Pitfall 4): change `activeCiv` (256) from `primaryCiv(snapshot)` to `snapshot ? (snapshot.civs.find(c => c.id === selectedCivId) ?? primaryCiv(snapshot)) : null`. ScorePanel/ResourcesPanel consume the rewired activeCiv. ColonyPanel (lists axolotls) should scope entities to the selected civ via the existing `entity.civ_id === activeCiv.id` idiom (266-273).
    3. Per-civ log filter (D-10, Pitfall 5): filter `recentLog` by `selectedCivId` using `entry.civ_id === selectedCivId` (the robust field added in Plan 01 — NOT name-string matching). Show all entries when `selectedCivId` is null. Keep it one combined chronological stream; each entry stays color-tagged + model-name-tagged.
    4. Log entry rendering (D-11/D-12): each "ai_decision" entry shows action (title) + rationale (body) always. Add a per-entry expand toggle that reveals `entry.reasoning` when present (Option B persisted field); hide the toggle entirely when `entry.reasoning` is empty/absent. Design the toggle simply (discretion) — collapsed by default (available, not noisy).
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | tail -5 && npm test -- CivilizationView 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CivilizationView.tsx contains a leaderboard component reading activeSnapshot and sorting by `score.total`
    - Leaderboard rows call `setSelectedCivId` onClick (grep match)
    - activeCiv derivation contains `selectedCivId` and `?? primaryCiv` fallback (grep match)
    - Log filter uses `civ_id` (grep `civ_id ===` or equivalent), not civ-name matching
    - Log entry renders `reasoning` behind a toggle (grep `reasoning`)
    - `npx tsc --noEmit` exits 0
    - vitest specs for ranking/collapsed-greyed/row-click->selectedCivId, log filter, and reasoning toggle pass
  </acceptance_criteria>
  <done>A persistent top-bar leaderboard ranks living civs and greys collapsed ones; row-click selects a civ that drives the observer panel and per-civ log filter; log entries show action+rationale with reasoning behind an expand toggle; tsc + specs green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: civPilotControls civId (additive) + controller tag via set_civ_controller (ARENA-02, ARENA-03)</name>
  <files>tauri-app/src/components/civilization/CivilizationView.tsx</files>
  <read_first>
    - tauri-app/src/components/civilization/CivilizationView.tsx (civPilotControls type 67-70 + impl 409-444)
    - tauri-app/src/bindings.ts (commands.setCivController from Plan 01)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md (civPilotControls section)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-RESEARCH.md (section 6, Assumption A1)
  </read_first>
  <behavior>
    - Test: existing `civPilotControls.start({ goal, possessId })` call still works (no civId) — back-compat (ARENA-02).
    - Test: `civPilotControls.start({ civId: "civ-2", controller: "codex" })` sets selectedCivId to "civ-2" and calls setCivController(sessionId, "civ-2", "codex").
  </behavior>
  <action>
    Per D-15 (drive one civ by id — additive scoping) and D-16/ARENA-03 (controller tag set at start; RESEARCH A1: Phase 1 scope = selection + attribution, not replacing civ-X's per-turn model loop).

    1. Extend the `civPilotControls` type (67-70) and impl (409-444) additively: add optional `civId?: string` and `controller?: string` to `start`'s options. Existing calls `start({goal, possessId, requesterId, continueAfterTask})` MUST keep working unchanged (ARENA-02 "without breaking existing controls") — do NOT make the new fields required and do NOT alter the possession behavior.
    2. When `civId` is provided: call `setSelectedCivId(civId)` so the harness's civ becomes the selected/observed one (the connective tissue from Plan 02). The other civs remain model-driven by `advance_civ_turn` (unchanged) — this is the "harness vs the models" framing (A1: scope selection + attribution; full decision-loop replacement is deferred).
    3. When `controller` is provided (D-16): call `commands.setCivController(activeSessionId, civId, controller)` (the new binding from Plan 01). The command sanitizes + persists the tag and re-emits a snapshot the listener auto-hydrates, so the leaderboard row badge (Task 1) and the text-state (Plan 03) both show it. Default controller can also be set at start; creation-time setting is out of scope for this task (discretion chose start-time).
    4. SECURITY (threat T-04-01): the controller value passed here is a free-form harness/model label, never a provider key; sanitization happens backend-side (Plan 01). Do not read config-map values to populate it.
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | tail -5 && npm test -- CivilizationView 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CivilizationView.tsx civPilotControls.start options type contains `civId?` AND `controller?`
    - start impl calls `setSelectedCivId` when civId present (grep match)
    - start impl calls `setCivController` when controller present (grep match)
    - existing start call sites (goal/possessId) still type-check (`npx tsc --noEmit` exits 0)
    - vitest spec: legacy start({goal,possessId}) works AND start({civId,controller}) sets selection + calls setCivController — passes
  </acceptance_criteria>
  <done>civPilotControls.start additively accepts civId + controller; a harness can scope-drive one civ and tag it, attributing its score on the leaderboard, without breaking existing controls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| window.civPilotControls (external harness) -> UI -> set_civ_controller IPC | An external harness invokes the bridge with a goal/civId/controller; these cross into the engine and get persisted. |
| Snapshot log (model output) -> reasoning toggle render | Model-produced rationale/reasoning text is rendered in the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing / Injection | controller tag from civPilotControls.start | mitigate | Tag is sanitized backend-side (Plan 01 T-01-02: trim + 64-char cap, drop-if-empty) before it reaches the leaderboard/text-state. UI passes it through; never sources it from the config map/keys. |
| T-04-02 | Injection (XSS) | model reasoning/rationale rendered in log toggle | mitigate | Render reasoning/rationale as React text children (auto-escaped), never via dangerouslySetInnerHTML. Model output is untrusted (RESEARCH security domain). |
| T-04-03 | Tampering | external harness driving via the bridge | accept | Driving uses the existing control surface additively; civId only scopes selection/attribution (A1), not the engine's per-civ decision loop. Local single-user desktop. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean against the regenerated bindings (Plan 01).
- `npm test -- CivilizationView` green (frontend runs on Windows).
- Back-compat (ARENA-02): existing `civPilotControls.start({goal,possessId})` spec stays green; no possession behavior change.
- Reasoning text rendered as escaped React children (no dangerouslySetInnerHTML — grep absence).
</verification>

<success_criteria>
- Persistent top-bar leaderboard ranks living civs by score and greys collapsed civs (CIV-02), updating as turns advance.
- Row-click selects a civ that drives the observer panel + per-civ log filter; per-civ decisions show action + rationale + expandable reasoning (CIV-03).
- civPilotControls.start additively scopes one civ by id and tags its controller for leaderboard attribution (ARENA-02, ARENA-03).
- tsc + vitest green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-04-SUMMARY.md`
</output>
