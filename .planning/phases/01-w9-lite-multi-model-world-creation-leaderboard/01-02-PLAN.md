---
phase: 01-w9-lite-multi-model-world-creation-leaderboard
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tauri-app/src/stores/civStore.ts
  - tauri-app/src/components/civilization/CivilizationView.tsx
autonomous: true
requirements: [CIV-01]
must_haves:
  truths:
    - "User can add 1-3 participant rows in the creation card, each with an editable name, a model picker, and an overridable color chip"
    - "Founding a world sends the multi-participant civs[] config to create_civ_session"
    - "A single participant still founds a world (back-compat with the legacy single-model path)"
    - "selectedCivId lives in civStore and resets on create/load"
  artifacts:
    - path: "tauri-app/src/stores/civStore.ts"
      provides: "Multi-participant createSession signature + selectedCivId state + setSelectedCivId + controller default in normalizeCiv"
      contains: "selectedCivId"
    - path: "tauri-app/src/components/civilization/CivilizationView.tsx"
      provides: "Multi-model participant picker (1-3 rows, name + model + color chip) wired to createSession"
      contains: "civs"
  key_links:
    - from: "creation card participant rows"
      to: "createSession({ name, seed, civs })"
      via: "handleCreate builds civs[] array"
      pattern: "createSession"
    - from: "createSession"
      to: "commands.createCivSession"
      via: "bindings.ts CivSessionConfig (regenerated in Plan 01)"
      pattern: "createCivSession"
---

<objective>
Build the multi-model world-creation flow (CIV-01) and the `selectedCivId` connective-tissue state. Replace the single-model `<select>` creation card with a 1-3 participant picker (each row: editable name, model picker, overridable color chip), wire it to a multi-participant `createSession`, and add `selectedCivId` to `civStore` (the shared selection that Plan 03's leaderboard/observer/log and Phase 2's `focusCiv` consume).

Purpose: This is the user-facing entry point for the now-multi-civ backend (Plan 01) and establishes the selection state every downstream panel reads.

Output: Updated `civStore.ts` (createSession + selectedCivId + controller default) and `CivilizationView.tsx` (participant picker); new/extended vitest specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-RESEARCH.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-CONTEXT.md
@CLAUDE.md
@tauri-app/src/bindings.ts

<interfaces>
<!-- Verified anchors. bindings.ts is regenerated by Plan 01 — read the regenerated CivSessionConfig/CivParticipant before coding. -->

civStore createSession (civStore.ts:26 type + 238-247 impl) — CURRENT signature `{ name; model; seed? }` passed straight to `commands.createCivSession(config)`.
CivState slice (civStore.ts:14-33) — already holds UI-ish state: lastEventType, turnRunning (init at ~215).
normalizeCiv (civStore.ts:115-135) — fills id, name, model, color (default "#6dd6a7"), alive, population, era, score.
loadModels()/listModels() (civStore.ts:219-226) — surfaces available model ids for the picker.
loadSession (civStore.ts:249), createSession (238) — selection should reset here.

Creation card (CivilizationView.tsx:805-838) — single-model <select> bound to selectedModel; name <Input> idiom at ~816; model <select> 817-827; "Found Colony" <Button disabled={loading || ...}> 828-831; handleCreate at ~533.
CIV palette: keep visually distinct legible hex chips (discretion). Mirror the backend CIV_COLORS ordering for the auto-assigned defaults so creation-time chips match the founded civ colors.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: civStore — multi-participant createSession + selectedCivId state + controller default</name>
  <files>tauri-app/src/stores/civStore.ts</files>
  <read_first>
    - tauri-app/src/stores/civStore.ts (CivState 14-33, createSession 26 + 238-247, loadSession 249, normalizeCiv 115-135, loadModels 219-226, init block ~215)
    - tauri-app/src/bindings.ts (regenerated CivSessionConfig + CivParticipant from Plan 01)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md (civStore section + "Single source of truth for selection" Shared Pattern)
  </read_first>
  <behavior>
    - Test: createSession({ name, seed, civs:[{name,model,color}] }) calls commands.createCivSession with a config carrying civs[] (mock the binding).
    - Test: setSelectedCivId("civ-2") updates store selectedCivId to "civ-2".
    - Test: createSession (and loadSession) resets selectedCivId to null (or top-ranked living civ — pick one and assert it).
    - Test: normalizeCiv defaults controller to null when absent.
  </behavior>
  <action>
    Per D-09 (selectedCivId is shared store state) and D-05 (multi-participant createSession).

    1. Change `createSession`'s typed signature (civStore.ts:26 + impl 238) from `{ name; model; seed? }` to the multi-participant shape: `(config: { name: string; seed?: number | null; civs: { name: string; model: string; color?: string | null }[] })`, passing it straight to `commands.createCivSession(config)` (the binding now accepts civs[] after Plan 01's regen). The legacy `model` field is no longer sent by the store; the backend Option<String> model purely guards old saved JSON / external callers.
    2. Add to the CivState slice (14-33): `selectedCivId: string | null;` and `setSelectedCivId: (id: string | null) => void;`. Initialize `selectedCivId: null` next to `turnRunning: false` (~215). Implement `setSelectedCivId: (id) => set({ selectedCivId: id })`.
    3. Reset selection on session change: inside `createSession` (238) and `loadSession` (249), `set({ selectedCivId: null })` (or set to the top-ranked living civ id — pick one consistently and reflect it in the test). Reset on any code path that swaps activeSessionId.
    4. In `normalizeCiv` (115-135), default the new controller field: `controller: input.controller ?? null` (or via the existing stringPropOrNull helper if present). This keeps the leaderboard/text-state reads safe before any controller is set.
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | tail -5 && npm test -- civStore 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - civStore.ts contains `selectedCivId` and `setSelectedCivId`
    - civStore.ts createSession signature contains `civs` (no longer `model:`)
    - civStore.ts normalizeCiv contains `controller`
    - `npx tsc --noEmit` exits 0
    - vitest store spec for selectedCivId + multi-participant createSession passes
  </acceptance_criteria>
  <done>civStore exposes multi-participant createSession and selectedCivId state, resets selection on session change, and defaults controller; tsc + store spec green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Creation card — 1-3 participant picker (name + model + color chip) wired to createSession (CIV-01)</name>
  <files>tauri-app/src/components/civilization/CivilizationView.tsx</files>
  <read_first>
    - tauri-app/src/components/civilization/CivilizationView.tsx (creation card 805-838, handleCreate ~533, selectedModel/models usage)
    - tauri-app/src/stores/civStore.ts (createSession after Task 1, loadModels)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md (CivilizationView creation card section)
  </read_first>
  <behavior>
    - Test (RTL/vitest): the creation card renders one participant row by default with an "add" affordance up to 3, and a "remove" affordance down to 1 (D-03 bounds; never 0, never >3).
    - Test: each row exposes an editable name input, a model select populated from models, and a color chip.
    - Test: clicking "Found" with 2 rows calls createSession with civs.length === 2 and the per-row name/model/color.
  </behavior>
  <action>
    Per D-01 (auto palette + override), D-02 (same model allowed across civs — no unique constraint), D-03 (1-3 participants), D-04 (auto names, inline-editable).

    1. Replace the single-model creation card (805-838) with a participant-rows UI. Maintain local state `participants: { name: string; model: string; color: string }[]` initialized to one row defaulting `name = "Civ 1"` (or first model name, D-04), `model = models[0]`, `color = PALETTE[0]`.
    2. Render each row mirroring the existing idioms: name `<Input>` (like 816), model `<select>` mapping `models.map(m => <option>)` (817-827 — NO unique-model filtering, D-02), and a color chip that defaults to `PALETTE[index]` and is overridable (D-01). Use a legible, visually-distinct palette (discretion) ordered to match backend CIV_COLORS so creation chips match founded colors.
    3. Add row controls: an "Add civilization" button enabled only while `participants.length < 3`; a per-row "remove" enabled only while `participants.length > 1` (D-03). New rows auto-assign the next palette color (D-01) and default name "Civ {n}" (D-04).
    4. Keep the "Found Colony" `<Button disabled={loading || ...}>` (828-831). Update `handleCreate` (~533) to build the config from participants: `createSession({ name: worldName, seed, civs: participants.map(p => ({ name: p.name.trim() || defaultName, model: p.model, color: p.color })) })`. Founding requires >=1 (always true given min 1 row).
    5. After founding, set selectedCivId via setSelectedCivId (top-ranked or null per Task 1's choice) so the observer panel has a selection.
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | tail -5 && npm test -- CivilizationView 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CivilizationView.tsx creation card references `participants` (array state) and renders a model `<select>` per row
    - CivilizationView.tsx handleCreate builds a `civs` array passed to createSession (grep `civs:` near the createSession call)
    - Add-row control gated by `< 3`; remove gated by `> 1` (grep the bounds)
    - `npx tsc --noEmit` exits 0
    - vitest creation-card spec (1-3 rows bounds, per-row name/model/color, Found calls createSession with N civs) passes
  </acceptance_criteria>
  <done>Creation card collects 1-3 participants with editable names, model pickers (same model allowed), and overridable color chips, and founds an N-civ world via createSession; tsc + component spec green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input (creation card fields) -> createSession -> IPC | Participant names, model selections, and color overrides are user-supplied and forwarded to the engine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering / DoS | participant count from UI | mitigate | UI enforces 1-3 rows (Task 2); backend also validates 1-3 (Plan 01 T-01-01) — defense in depth. |
| T-02-02 | Information disclosure | model picker shows config models | accept | loadModels()/listModels() returns only model ids, never key values (gotcha #2). No key material reaches the UI. |
| T-02-03 | Tampering | color chip value | accept | Color is a cosmetic hex string rendered as a swatch; no code execution path. Backend stores as-is on the civ. Low risk. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes against the regenerated bindings (Plan 01).
- vitest store + component specs green (run on Windows fine — frontend only).
- Back-compat: a single participant row founds a world (exercises the one-element civs[] path that maps to the legacy behavior backend-side).
</verification>

<success_criteria>
- User can create a world with 1-3 participants, each with an editable name, model picker (same model allowed), and overridable color chip, and found it (CIV-01).
- selectedCivId is store-owned, set on found, and reset on session change.
- tsc + vitest green.
</success_criteria>

<output>
After completion, create `.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-02-SUMMARY.md`
</output>
