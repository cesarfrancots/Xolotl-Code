---
phase: "06"
plan: "03"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - tauri-app/src/components/agent/AgentCard.tsx
  - tauri-app/src/components/agent/AgentPanel.tsx
  - tauri-app/src/components/agent/LaunchTeamDialog.tsx
autonomous: true
requirements:
  - WRK-01
  - WRK-02
  - WRK-03
  - WRK-05

must_haves:
  truths:
    - "AgentCard shows branch label below task text when agent.branch is non-empty"
    - "AgentCard shows pulsing blue dot beside badge when agent.state === 'Executing'"
    - "AgentPanel shows group header rows above grouped agents with name, progress count, and View Checkpoint button"
    - "AgentPanel header has Users icon button that opens LaunchTeamDialog"
    - "LaunchTeamDialog Team mode has 4 role rows (Planner/Coder/Reviewer/Tester) with model Select + textarea"
    - "LaunchTeamDialog Swarm mode has count input (1-8), objective textarea, model Select"
    - "LaunchTeamDialog submitting invokes commands.launchTeam / launchSwarm and calls agentStore.addGroup + addAgent for each result"
    - "useGroupWatcher is mounted inside AgentPanel"
  artifacts:
    - path: "tauri-app/src/components/agent/AgentCard.tsx"
      provides: "Branch label chip + pulsing badge"
      contains: "animate-ping"
    - path: "tauri-app/src/components/agent/AgentPanel.tsx"
      provides: "Group header rows + Users button + useGroupWatcher mount"
      contains: "useGroupWatcher"
    - path: "tauri-app/src/components/agent/LaunchTeamDialog.tsx"
      provides: "Team/Swarm launch dialog"
      contains: "LaunchTeamDialog"
  key_links:
    - from: "tauri-app/src/components/agent/LaunchTeamDialog.tsx"
      to: "tauri-app/src/bindings.ts"
      via: "commands.launchTeam / commands.launchSwarm"
      pattern: "commands.launchTeam"
    - from: "tauri-app/src/components/agent/AgentPanel.tsx"
      to: "tauri-app/src/hooks/useGroupWatcher.ts"
      via: "useGroupWatcher() call in component body"
      pattern: "useGroupWatcher"
---

<objective>
Add all Wave 3 UI chrome: branch label and pulsing badge on AgentCard, group header rows in AgentPanel, Users button, and the new LaunchTeamDialog component.

Purpose: This wave makes team/swarm orchestration visible and launchable from the UI. AgentCard and AgentPanel changes are independent of LaunchTeamDialog but share the same wave since no file overlap exists.

Output: Updated AgentCard.tsx, updated AgentPanel.tsx (with useGroupWatcher mounted), and new LaunchTeamDialog.tsx.
</objective>

<execution_context>
@C:\Users\zazuk\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\zazuk\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-parallel-worktrees-team-orchestration/06-CONTEXT.md
@.planning/phases/06-parallel-worktrees-team-orchestration/06-UI-SPEC.md
@.planning/phases/06-parallel-worktrees-team-orchestration/06-02-SUMMARY.md

<interfaces>
<!-- Key shapes to replicate. Extracted from codebase audit. -->

From tauri-app/src/components/agent/AgentCard.tsx (current structure):
```tsx
export function AgentCard({ agent }: { agent: AgentRecord }) {
  // Top row: AgentStateBadge + cost
  // Middle: task text paragraph
  // Bottom row: Stop + Expand buttons
}
// Pulsing dot goes in top row, after <AgentStateBadge>, with ml-1
// Branch label goes after task <p>, before bottom button row
```

From tauri-app/src/components/agent/AgentPanel.tsx (current structure):
```tsx
export function AgentPanel() {
  const agents = useAgentStore((s) => s.agents);
  const [dialogOpen, setDialogOpen] = useState(false);
  // flat agents.map() — must become grouped rendering
  // SpawnAgentDialog is already mounted here
}
// Groups from agentStore.groups; group header rendered above each group's cards
// Ungrouped agents (groupId === null) render as flat list after grouped sections
```

From tauri-app/src/components/agent/SpawnAgentDialog.tsx (dialog pattern to replicate):
```tsx
// shadcn Dialog + DialogContent + DialogHeader + DialogTitle + DialogDescription
// DialogFooter with ghost Discard + primary Launch
// Select for models, textarea for input
// useEffect(() => { commands.listModels()... }, []) for model list
// commands.spawnAgent(task, model, budget) → result.status check
```

From tauri-app/src/bindings.ts (Wave 1 additions):
```typescript
launchTeam: (roles: RoleConfig[]) => typedError<GroupLaunchResult, string>(...)
launchSwarm: (count: number, objective: string, model: string) => typedError<GroupLaunchResult, string>(...)
export type RoleConfig = { role: string; task: string; model: string; };
export type GroupLaunchResult = { group_id: string; agent_ids: string[]; branches: string[]; };
```

From UI-SPEC (exact CSS classes for AgentCard additions):
```
Branch label: className="text-xs font-mono text-[oklch(0.45_0_0)] mt-1 truncate block max-w-full"
Pulsing dot wrapper: className="relative flex h-2 w-2 ml-1"
Pulsing ring: className="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"
Pulsing center: className="relative inline-flex rounded-full h-2 w-2 bg-blue-500"

Group header: className="flex items-center justify-between px-3 py-2 mx-2 mt-2 rounded-t-md bg-[oklch(0.18_0_0)] border border-neutral-800 border-b-0"
Group name: className="text-xs font-semibold text-[oklch(0.92_0_0)] truncate"
Group status: className="text-xs text-[oklch(0.55_0_0)] flex-none"
View Checkpoint: variant="ghost" size="sm" className="h-6 text-xs px-2 flex-none"
Agent cards in group wrapper: className="mx-2 border-x border-b border-neutral-800 rounded-b-md"

Users button: variant="ghost" size="icon" className="h-7 w-7" title="Launch team or swarm"

Dialog mode toggle pill container: className="flex gap-1 p-1 rounded-md bg-[oklch(0.20_0_0)] w-fit"
Active mode button: className="rounded-sm px-3 py-1 text-xs font-medium transition-colors bg-[oklch(0.28_0_0)] text-[oklch(0.92_0_0)]"
Inactive mode button: className="rounded-sm px-3 py-1 text-xs font-medium transition-colors text-[oklch(0.55_0_0)]"

Role row: className="flex flex-col gap-2 py-3 border-b border-neutral-800 last:border-0"
Role textarea: className="bg-[oklch(0.20_0_0)] border border-neutral-800 rounded-md px-3 py-2 text-sm text-[oklch(0.92_0_0)] resize-none w-full"
Role Select trigger: className="w-44 h-7 text-xs bg-[oklch(0.20_0_0)] border-neutral-800"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AgentCard (branch label + pulsing badge)</name>
  <read_first>
    - tauri-app/src/components/agent/AgentCard.tsx (full file — must understand exact JSX structure to insert correctly)
    - tauri-app/src/stores/agentStore.ts (confirm AgentRecord now has branch: string after Wave 2)
  </read_first>
  <files>
    tauri-app/src/components/agent/AgentCard.tsx
  </files>
  <action>
    Make two surgical insertions to AgentCard.tsx:

    **Insertion 1 — Pulsing dot** in the top row (the `flex items-center justify-between gap-2` div):
    After `<AgentStateBadge state={agent.state} />`, insert:
    ```tsx
    {agent.state === "Executing" && (
      <span className="relative flex h-2 w-2 ml-1">
        <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75" />
        <span className="relative inline-flex rounded-full h-2 w-2 bg-blue-500" />
      </span>
    )}
    ```

    **Insertion 2 — Branch label** after the task `<p>` element, before the button row `<div className="flex items-center justify-end gap-1 mt-1">`:
    ```tsx
    {agent.branch && (
      <code className="text-xs font-mono text-[oklch(0.45_0_0)] mt-1 truncate block max-w-full">
        {agent.branch}
      </code>
    )}
    ```

    No other changes. Do NOT touch the Stop button, Expand button, cost display, or import list.
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "animate-ping" tauri-app/src/components/agent/AgentCard.tsx finds a match
    - grep "agent.branch" tauri-app/src/components/agent/AgentCard.tsx finds a match
    - grep "oklch(0.45_0_0)" tauri-app/src/components/agent/AgentCard.tsx finds a match
    - grep "font-mono" tauri-app/src/components/agent/AgentCard.tsx finds a match
    - cd tauri-app && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AgentCard shows branch label chip when branch is non-empty; pulsing blue dot appears when state is Executing; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update AgentPanel (group headers + Users button + useGroupWatcher) + Create LaunchTeamDialog</name>
  <read_first>
    - tauri-app/src/components/agent/AgentPanel.tsx (full file — understand current structure before rewriting roster section)
    - tauri-app/src/components/agent/SpawnAgentDialog.tsx (full file — dialog pattern to replicate for LaunchTeamDialog)
    - tauri-app/src/stores/agentStore.ts (after Wave 2 — confirm groups and mergeCheckpointGroupId available)
    - tauri-app/src/hooks/useGroupWatcher.ts (confirm hook signature)
  </read_first>
  <files>
    tauri-app/src/components/agent/AgentPanel.tsx,
    tauri-app/src/components/agent/LaunchTeamDialog.tsx
  </files>
  <action>
    **AgentPanel.tsx changes:**

    1. Add imports at top:
    ```tsx
    import { Plus, Users } from "lucide-react";
    import { useGroupWatcher } from "../../hooks/useGroupWatcher";
    import { LaunchTeamDialog } from "./LaunchTeamDialog";
    ```

    2. Add state for team dialog:
    ```tsx
    const [teamDialogOpen, setTeamDialogOpen] = useState(false);
    ```

    3. Add selectors for groups and openMergeCheckpoint:
    ```tsx
    const groups = useAgentStore((s) => s.groups);
    const openMergeCheckpoint = useAgentStore((s) => s.openMergeCheckpoint);
    ```

    4. Mount the group watcher hook in the component body:
    ```tsx
    useGroupWatcher();
    ```

    5. Update the header to include the Users button to the LEFT of Plus:
    ```tsx
    <div className="flex items-center justify-between px-4 h-12 flex-none border-b border-neutral-800">
      <span className="text-xs font-normal text-[oklch(0.55_0_0)]">AGENTS</span>
      <div className="flex items-center gap-1">
        <Button
          variant="ghost"
          size="icon"
          className="h-7 w-7"
          title="Launch team or swarm"
          aria-label="Launch team or swarm"
          onClick={() => setTeamDialogOpen(true)}
        >
          <Users className="h-4 w-4" />
        </Button>
        <Button
          variant="ghost"
          size="icon"
          className="h-7 w-7"
          title="New agent"
          onClick={() => setDialogOpen(true)}
        >
          <Plus className="h-4 w-4" />
        </Button>
      </div>
    </div>
    ```

    6. Replace the roster rendering section. The flat `agents.map()` becomes:

    Build a helper inside the component to compute grouped rendering:
    ```tsx
    // Compute group → agents map and ungrouped agents
    const agentsByGroupId = new Map<string, typeof agents[0][]>();
    const ungroupedAgents: typeof agents[0][] = [];
    for (const agent of agents) {
      if (agent.groupId) {
        const arr = agentsByGroupId.get(agent.groupId) ?? [];
        arr.push(agent);
        agentsByGroupId.set(agent.groupId, arr);
      } else {
        ungroupedAgents.push(agent);
      }
    }
    ```

    Replace the roster div with:
    ```tsx
    <div className="flex flex-col py-2">
      {/* Grouped agents */}
      {groups.map((group) => {
        const groupAgents = agentsByGroupId.get(group.id) ?? [];
        const doneCount = groupAgents.filter(
          (a) => a.state === "Done" || a.state === "Failed"
        ).length;
        return (
          <div key={group.id}>
            {/* Group header */}
            <div className="flex items-center justify-between px-3 py-2 mx-2 mt-2 rounded-t-md bg-[oklch(0.18_0_0)] border border-neutral-800 border-b-0">
              <div className="flex items-center gap-2 min-w-0">
                <span className="text-xs font-semibold text-[oklch(0.92_0_0)] truncate">{group.name}</span>
                <span className="text-xs text-[oklch(0.55_0_0)] flex-none">
                  {doneCount}/{groupAgents.length} Done
                </span>
              </div>
              {(group.mergeState === "AllDone" || group.mergeState === "CheckpointOpen") && (
                <Button
                  variant="ghost"
                  size="sm"
                  className="h-6 text-xs px-2 flex-none"
                  onClick={() => openMergeCheckpoint(group.id)}
                >
                  View Checkpoint
                </Button>
              )}
            </div>
            {/* Agent cards in group envelope */}
            <div className="mx-2 border-x border-b border-neutral-800 rounded-b-md">
              {groupAgents.map((agent) => (
                <AgentCard key={agent.id} agent={agent} />
              ))}
            </div>
          </div>
        );
      })}
      {/* Ungrouped agents */}
      {ungroupedAgents.map((agent) => (
        <AgentCard key={agent.id} agent={agent} />
      ))}
    </div>
    ```

    7. Add `<LaunchTeamDialog>` below `<SpawnAgentDialog>`:
    ```tsx
    <LaunchTeamDialog open={teamDialogOpen} onOpenChange={setTeamDialogOpen} />
    ```

    8. Keep the empty state (no agents AND no groups) block unchanged.

    ---

    **Create tauri-app/src/components/agent/LaunchTeamDialog.tsx** (new file):

    ```tsx
    import { useEffect, useState } from "react";
    import {
      Dialog,
      DialogContent,
      DialogHeader,
      DialogTitle,
      DialogFooter,
      DialogDescription,
    } from "../ui/dialog";
    import { Button } from "../ui/button";
    import {
      Select,
      SelectContent,
      SelectItem,
      SelectTrigger,
      SelectValue,
    } from "../ui/select";
    import { commands, type RoleConfig } from "../../bindings";
    import { useAgentStore } from "../../stores/agentStore";

    type Mode = "team" | "swarm";

    const ROLES = [
      { name: "Planner", placeholder: "e.g. Break the feature into subtasks and define acceptance criteria" },
      { name: "Coder",   placeholder: "e.g. Implement the feature based on the plan" },
      { name: "Reviewer",placeholder: "e.g. Review the implementation for bugs and code quality" },
      { name: "Tester",  placeholder: "e.g. Write tests to verify the implementation" },
    ] as const;

    interface TeamRole {
      task: string;
      model: string;
    }

    export function LaunchTeamDialog({
      open,
      onOpenChange,
    }: {
      open: boolean;
      onOpenChange: (open: boolean) => void;
    }) {
      const [models, setModels] = useState<string[]>([]);
      const [mode, setMode] = useState<Mode>("team");
      const [error, setError] = useState<string | null>(null);
      const [submitting, setSubmitting] = useState(false);

      // Team mode state — one entry per role
      const [teamRoles, setTeamRoles] = useState<TeamRole[]>(
        ROLES.map(() => ({ task: "", model: "" }))
      );

      // Swarm mode state
      const [swarmCount, setSwarmCount] = useState<string>("2");
      const [swarmObjective, setSwarmObjective] = useState<string>("");
      const [swarmModel, setSwarmModel] = useState<string>("");

      useEffect(() => {
        commands
          .listModels()
          .then((list) => {
            if (list.length > 0) {
              setModels(list);
              setSwarmModel(list[0]);
              setTeamRoles((roles) => roles.map((r) => ({ ...r, model: list[0] })));
            }
          })
          .catch(() => setError("Failed to load model list."));
      }, []);

      function reset() {
        setMode("team");
        setError(null);
        setSwarmCount("2");
        setSwarmObjective("");
        setTeamRoles(ROLES.map(() => ({ task: "", model: models[0] ?? "" })));
      }

      function updateRoleTask(index: number, task: string) {
        setTeamRoles((roles) => roles.map((r, i) => i === index ? { ...r, task } : r));
      }

      function updateRoleModel(index: number, model: string) {
        setTeamRoles((roles) => roles.map((r, i) => i === index ? { ...r, model } : r));
      }

      async function handleLaunch() {
        setError(null);

        if (mode === "team") {
          const anyEmpty = teamRoles.some((r) => !r.task.trim());
          if (anyEmpty) {
            setError("All role tasks are required.");
            return;
          }
          const roles: RoleConfig[] = ROLES.map((roleDef, i) => ({
            role: roleDef.name,
            task: teamRoles[i].task.trim(),
            model: teamRoles[i].model,
          }));
          setSubmitting(true);
          const result = await commands.launchTeam(roles);
          setSubmitting(false);
          if (result.status === "error") {
            setError(`Failed to launch: ${result.error}`);
            return;
          }
          const { group_id, agent_ids, branches } = result.data;
          const groupName = roles[0].task.slice(0, 40) || "Team";
          useAgentStore.getState().addGroup(group_id, agent_ids, "team", groupName);
          agent_ids.forEach((id, i) => {
            useAgentStore.getState().addAgent(id, roles[i].task, roles[i].model, branches[i], group_id);
          });
        } else {
          const count = Number(swarmCount);
          if (!Number.isFinite(count) || count < 1 || count > 8) {
            setError("Agent count must be between 1 and 8.");
            return;
          }
          if (!swarmObjective.trim()) {
            setError("Objective is required.");
            return;
          }
          setSubmitting(true);
          const result = await commands.launchSwarm(count, swarmObjective.trim(), swarmModel);
          setSubmitting(false);
          if (result.status === "error") {
            setError(`Failed to launch: ${result.error}`);
            return;
          }
          const { group_id, agent_ids, branches } = result.data;
          const groupName = swarmObjective.slice(0, 40) || "Swarm";
          useAgentStore.getState().addGroup(group_id, agent_ids, "swarm", groupName);
          agent_ids.forEach((id, i) => {
            useAgentStore.getState().addAgent(id, swarmObjective.trim(), swarmModel, branches[i], group_id);
          });
        }

        reset();
        onOpenChange(false);
      }

      const teamLaunchDisabled = submitting || teamRoles.some((r) => !r.task.trim());
      const swarmLaunchDisabled = submitting || !swarmObjective.trim();

      return (
        <Dialog
          open={open}
          onOpenChange={(o) => {
            if (!o) reset();
            onOpenChange(o);
          }}
        >
          <DialogContent className="bg-[oklch(0.16_0_0)] border-neutral-800 text-[oklch(0.92_0_0)] sm:max-w-[560px]">
            <DialogHeader>
              <DialogTitle>Launch Team</DialogTitle>
              <DialogDescription className="text-[oklch(0.55_0_0)]">
                Configure a role-based team or a parallel swarm.
              </DialogDescription>
            </DialogHeader>

            {/* Mode toggle */}
            <div className="flex gap-1 p-1 rounded-md bg-[oklch(0.20_0_0)] w-fit">
              <button
                className={`rounded-sm px-3 py-1 text-xs font-medium transition-colors ${
                  mode === "team"
                    ? "bg-[oklch(0.28_0_0)] text-[oklch(0.92_0_0)]"
                    : "text-[oklch(0.55_0_0)]"
                }`}
                onClick={() => setMode("team")}
              >
                Team
              </button>
              <button
                className={`rounded-sm px-3 py-1 text-xs font-medium transition-colors ${
                  mode === "swarm"
                    ? "bg-[oklch(0.28_0_0)] text-[oklch(0.92_0_0)]"
                    : "text-[oklch(0.55_0_0)]"
                }`}
                onClick={() => setMode("swarm")}
              >
                Swarm
              </button>
            </div>

            {/* Team form */}
            {mode === "team" && (
              <div className="flex flex-col gap-0 py-1 max-h-[60vh] overflow-y-auto">
                {ROLES.map((roleDef, i) => (
                  <div key={roleDef.name} className="flex flex-col gap-2 py-3 border-b border-neutral-800 last:border-0">
                    <div className="flex items-center justify-between">
                      <span className="text-xs font-semibold text-[oklch(0.92_0_0)]">{roleDef.name}</span>
                      <Select
                        value={teamRoles[i].model}
                        onValueChange={(v) => updateRoleModel(i, v)}
                      >
                        <SelectTrigger className="w-44 h-7 text-xs bg-[oklch(0.20_0_0)] border-neutral-800">
                          <SelectValue />
                        </SelectTrigger>
                        <SelectContent className="bg-[oklch(0.16_0_0)] border-neutral-800">
                          {models.map((m) => (
                            <SelectItem key={m} value={m}>{m}</SelectItem>
                          ))}
                        </SelectContent>
                      </Select>
                    </div>
                    <textarea
                      rows={2}
                      value={teamRoles[i].task}
                      onChange={(e) => updateRoleTask(i, e.target.value)}
                      placeholder={roleDef.placeholder}
                      className="bg-[oklch(0.20_0_0)] border border-neutral-800 rounded-md px-3 py-2 text-sm text-[oklch(0.92_0_0)] resize-none w-full"
                    />
                  </div>
                ))}
              </div>
            )}

            {/* Swarm form */}
            {mode === "swarm" && (
              <div className="flex flex-col gap-4 py-2">
                <div className="flex flex-col gap-1">
                  <label className="text-xs text-[oklch(0.55_0_0)]">Agent count</label>
                  <input
                    type="number"
                    min="1"
                    max="8"
                    value={swarmCount}
                    onChange={(e) => setSwarmCount(e.target.value)}
                    placeholder="2"
                    className="bg-[oklch(0.20_0_0)] border border-neutral-800 rounded-md px-3 py-2 text-sm text-[oklch(0.92_0_0)] w-24"
                  />
                </div>
                <div className="flex flex-col gap-1">
                  <label className="text-xs text-[oklch(0.55_0_0)]">Shared objective</label>
                  <textarea
                    rows={4}
                    value={swarmObjective}
                    onChange={(e) => setSwarmObjective(e.target.value)}
                    placeholder="e.g. Explore three different approaches to optimizing the query pipeline"
                    className="bg-[oklch(0.20_0_0)] border border-neutral-800 rounded-md px-3 py-2 text-sm text-[oklch(0.92_0_0)] resize-none"
                  />
                </div>
                <div className="flex flex-col gap-1">
                  <label className="text-xs text-[oklch(0.55_0_0)]">Model (all agents)</label>
                  <Select value={swarmModel} onValueChange={setSwarmModel}>
                    <SelectTrigger className="bg-[oklch(0.20_0_0)] border-neutral-800">
                      <SelectValue />
                    </SelectTrigger>
                    <SelectContent className="bg-[oklch(0.16_0_0)] border-neutral-800">
                      {models.map((m) => (
                        <SelectItem key={m} value={m}>{m}</SelectItem>
                      ))}
                    </SelectContent>
                  </Select>
                </div>
              </div>
            )}

            {error && (
              <p className="text-xs text-red-400" role="alert">
                {error}
              </p>
            )}

            <DialogFooter>
              <Button variant="ghost" onClick={() => onOpenChange(false)}>
                Discard
              </Button>
              <Button
                onClick={() => void handleLaunch()}
                disabled={mode === "team" ? teamLaunchDisabled : swarmLaunchDisabled}
              >
                {submitting ? "Launching…" : mode === "team" ? "Launch Team" : "Launch Swarm"}
              </Button>
            </DialogFooter>
          </DialogContent>
        </Dialog>
      );
    }
    ```
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep "useGroupWatcher" tauri-app/src/components/agent/AgentPanel.tsx finds a match (mounted in component body)
    - grep "LaunchTeamDialog" tauri-app/src/components/agent/AgentPanel.tsx finds a match (imported and rendered)
    - grep "Users" tauri-app/src/components/agent/AgentPanel.tsx finds a match (lucide icon imported and used)
    - grep "View Checkpoint" tauri-app/src/components/agent/AgentPanel.tsx finds a match
    - grep "border-b-0" tauri-app/src/components/agent/AgentPanel.tsx finds a match (group header visual envelope)
    - tauri-app/src/components/agent/LaunchTeamDialog.tsx exists
    - grep "launchTeam\|launchSwarm" tauri-app/src/components/agent/LaunchTeamDialog.tsx finds matches
    - grep "Planner\|Coder\|Reviewer\|Tester" tauri-app/src/components/agent/LaunchTeamDialog.tsx finds matches
    - grep "Launch Team\|Launch Swarm" tauri-app/src/components/agent/LaunchTeamDialog.tsx finds matches
    - grep "\"All role tasks are required\.\"\|\"Agent count must be between 1 and 8\.\"\|\"Objective is required\.\"" tauri-app/src/components/agent/LaunchTeamDialog.tsx finds 3 matches (validation messages per UI-SPEC)
    - cd tauri-app && npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AgentPanel has Users button, group headers, useGroupWatcher mounted; LaunchTeamDialog handles Team and Swarm modes with exact copy from UI-SPEC; TypeScript compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI form → Tauri IPC | User-entered task text and count from LaunchTeamDialog forms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | DoS | LaunchTeamDialog swarm count | mitigate | Frontend validates count 1-8 before calling launchSwarm; Rust backend also validates (defense in depth) |
| T-06-08 | Tampering | LaunchTeamDialog role task text | accept | Task text becomes branch names via slugify_task() in Rust — user input never used verbatim as a git argument |
</threat_model>

<verification>
After both tasks complete:
1. `cd tauri-app && npx tsc --noEmit` — no TypeScript errors
2. `grep "animate-ping" tauri-app/src/components/agent/AgentCard.tsx` — finds match
3. `grep "useGroupWatcher" tauri-app/src/components/agent/AgentPanel.tsx` — finds match
4. `ls tauri-app/src/components/agent/LaunchTeamDialog.tsx` — file exists
5. `grep "launchTeam" tauri-app/src/components/agent/LaunchTeamDialog.tsx` — finds match
</verification>

<success_criteria>
- AgentCard: branch label chip shows when agent.branch is non-empty; pulsing blue dot shows when state is Executing; exact CSS classes from UI-SPEC used
- AgentPanel: Users icon button opens LaunchTeamDialog; group header rows render above grouped agents with name + progress + View Checkpoint button; useGroupWatcher() mounted
- LaunchTeamDialog: Team mode has 4 role rows (Planner/Coder/Reviewer/Tester) with model Select + textarea; Swarm mode has count input + objective + model; submitting invokes IPC and updates agentStore; all error strings match UI-SPEC exactly
- TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/06-parallel-worktrees-team-orchestration/06-03-SUMMARY.md`
</output>
