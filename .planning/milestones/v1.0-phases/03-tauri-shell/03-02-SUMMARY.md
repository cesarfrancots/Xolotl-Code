---
plan: "03-02"
phase: "03-tauri-shell"
status: complete
completed: 2026-05-08
---

# Summary: specta::Type Derives + Minimal IPC + TypeScript Bindings

## What Was Built

Added `specta::Type` derives to runtime types, added `AlwaysAllow` to `PermissionPromptDecision`, wrote a minimal `lib.rs` + `commands.rs` wiring the tauri-specta Builder, and generated the initial `tauri-app/src/bindings.ts`.

## Tasks Completed

| # | Task | Commit |
|---|------|--------|
| 1 | specta::Type on AgentId/AgentState/AgentEvent/TokenUsage + AlwaysAllow | 803f357 |
| 2 | Minimal lib.rs + commands.rs + bindings.ts generation | f7a0b83 |

## Key Files

### Created
- `tauri-app/src-tauri/src/commands.rs` — smoke_test() with `#[tauri::command]` + `#[specta::specta]`
- `tauri-app/src/bindings.ts` — generated TypeScript: smokeTest command, AgentState union, AgentId=string, AgentEvent tagged union, TokenUsage interface

### Modified
- `rust/crates/runtime/Cargo.toml` — specta dep updated to `features = ["derive"]`
- `rust/crates/runtime/src/supervisor/agent_state.rs` — `#[derive(specta::Type)]` on AgentId, AgentState, AgentEvent
- `rust/crates/runtime/src/usage.rs` — `#[derive(specta::Type)]` on TokenUsage (required by AgentEvent::TurnCompleted)
- `rust/crates/runtime/src/permissions.rs` — AlwaysAllow variant added to PermissionPromptDecision; authorize() maps it to PermissionOutcome::Allow
- `tauri-app/src-tauri/src/lib.rs` — make_builder() + export_bindings() + run() with Builder pattern

## Deviations

1. **specta::Type import path**: `use specta::Type;` shadows the derive macro with the trait. Used `#[derive(specta::Type)]` (full path) instead — correct pattern per specta rc.25 docs.
2. **runtime::supervisor module private**: Corrected import from `runtime::supervisor::{...}` to `runtime::{...}` (types are re-exported at root).
3. **bindings.ts generation via app run**: `generate_context!()` macro requires `dist/` to exist at compile time. Created local `dist/index.html` stub (gitignored); bindings.ts generated by running the debug binary once. The `export_bindings()` helper is also available for test-driven regeneration when WebView2 DLL issue is resolved.

## Self-Check: PASSED

- ✅ `specta::Type` implemented on AgentId, AgentState, AgentEvent, TokenUsage
- ✅ AlwaysAllow variant in PermissionPromptDecision
- ✅ tauri-app/src-tauri compiles with lib.rs wiring smoke_test command
- ✅ tauri-app/src/bindings.ts committed and exports AgentState, smokeTest
- ✅ `tsc --noEmit` passes in tauri-app/
- ✅ rust/ workspace tests pass (compat-harness failures are pre-existing)
