---
phase: 03-w4-environment-engine
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - tauri-app/src-tauri/src/civilization.rs
autonomous: true
requirements: [ENV-01, ENV-02, ENV-03]
must_haves:
  truths:
    - "tick_environment runs once per turn at TURN START (after turn increment, before the civ decision loop) so civs observe the freshly-advanced season/temperature/forecast/disaster the same turn (ENV-01/02/03)"
    - "tick_environment runs the CONTEXT-locked sequence: fire any due forecast (reshape terrain + push reused CivModifier + log) → advance season/temp/water (log on wrap) → regrow resources → tick disaster countdowns (log expiry) → roll/refresh the forecast (log announce)"
    - "Every forecast announce, disaster fire, season change, and disaster expiry is logged via push_log with a distinct kind so the harness/UI can filter (ENV-01/02)"
    - "The full env tick is byte-deterministic: running tick_environment twice on cloned snapshots yields serde-identical environment + world.tiles (deterministic replay)"
  artifacts:
    - path: "tauri-app/src-tauri/src/civilization.rs"
      provides: "tick_environment orchestrator + its insertion in advance_civ_turn at line 794; integration + byte-determinism tests over a test_snapshot"
      contains: "fn tick_environment"
  key_links:
    - from: "advance_civ_turn"
      to: "tick_environment"
      via: "tick_environment(&mut snapshot); inserted after snapshot.turn = next_turn (line 794), before civ_turn_order (line 798)"
      pattern: "tick_environment\\(&mut snapshot\\)"
    - from: "tick_environment"
      to: "advance_season / regrow_resources / roll_forecast / apply_disaster_to_tiles"
      via: "the locked sequence calling the Wave-1/2 pure helpers"
      pattern: "advance_season|regrow_resources|roll_forecast|apply_disaster_to_tiles"
    - from: "fired disaster"
      to: "resolve_environment drought/cold_snap arms"
      via: "push CivModifier (reused kind) → applied by existing post-loop resolve_environment + tick_modifiers"
      pattern: "CivModifier"
---

<objective>
Wire the four Wave-1/2 pure helpers into a single per-turn orchestrator `tick_environment(&mut snapshot)` and insert it at TURN START in `advance_civ_turn` (after `snapshot.turn = next_turn;` at line 794, before `civ_turn_order` at line 798). `tick_environment` runs the CONTEXT-locked sequence — fire due forecast (reshape terrain + push a reused `CivModifier` + log) → advance season (log on wrap) → regrow resources → tick disaster countdowns (log expiry) → roll/refresh the forecast (log announce) — and logs every world-event so the already-wired frontend/harness surface live data with zero frontend changes. Backed by integration tests over a `test_snapshot` and a byte-determinism test (run twice, serde-compare).

Purpose: This is the load-bearing plan — it turns the four isolated, tested helpers into observable end-to-end behavior (the world stops being stale: seasons drift, disasters forecast→fire→reshape, renewables recover while finite stays depleted). It MUST run after Waves 1+2 because it calls all four helpers and they all live in the same file. Turn-start placement is load-bearing: `build_observation` (per-civ, in the decision loop) then shows the freshly-advanced season/forecast, and a fired disaster's `CivModifier` rides the EXISTING post-loop `resolve_environment` (870) + `tick_modifiers` (884).

Output: Updated `civilization.rs` with `tick_environment`, the one-line insertion in `advance_civ_turn`, and integration + byte-determinism tests proving the sequence, logging, and ENV-01/02/03 behaviors end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-w4-environment-engine/03-CONTEXT.md
@.planning/phases/03-w4-environment-engine/03-RESEARCH.md
@.planning/phases/03-w4-environment-engine/03-PATTERNS.md
@.planning/phases/03-w4-environment-engine/03-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Exact shapes the executor must use. Verified against civilization.rs at HEAD 2026-06-07. -->

Wave-1/2 helpers this plan ORCHESTRATES (signatures fixed by 03-01 + 03-02):
```rust
fn advance_season(season:&str, turn_of_season:u32, temperature:f32, water_level:i32, turn:u32, seed:u32) -> (String,u32,f32,i32);  // 03-01
fn regrow_resources(tiles:&mut [CivTile], season:&str, temperature:f32);                                                            // 03-01
fn roll_forecast(seed:u32, turn:u32, env:&CivEnvironment, world_width:u32) -> Option<CivDisaster>;                                   // 03-02
fn apply_disaster_to_tiles(tiles:&mut [CivTile], dis:&CivDisaster, width:u32);                                                       // 03-02
```
Convention from 03-02: a CivDisaster in `env.forecast` carries `remaining_turns` = the FORECAST LEAD countdown. When it fires, reset `remaining_turns` to its ACTIVE duration.

CivSessionSnapshot fields used (civilization.rs:283-305): `seed: u32`, `turn: u32`, `world: CivWorld`, `environment: CivEnvironment`, `modifiers: Vec<CivModifier>`, `log: Vec<CivLogEntry>`. CivWorld.width (309), CivWorld.tiles (311).

CivEnvironment (civilization.rs:491-500): season, turn_of_season, temperature, water_level, disasters: Vec<CivDisaster>, forecast: Option<CivDisaster>.

push_log (civilization.rs:4450) — handles the 240-entry ring-buffer trim; use distinct kinds:
```rust
fn push_log(snapshot: &mut CivSessionSnapshot, kind: &str, title: &str, body: &str);
```
Existing log kinds in use include "terraform" (2307), "crisis", "collapse"; this plan adds "season", "forecast", "disaster" (distinct so harness/UI can filter).

tick_modifiers expiry pattern to MIRROR for disaster countdowns (civilization.rs:2586-2593):
```rust
for modifier in snapshot.modifiers.iter_mut() { modifier.remaining_turns = modifier.remaining_turns.saturating_sub(1); }
snapshot.modifiers.retain(|m| m.remaining_turns > 0);
```

resolve_environment drought/cold_snap arms ALREADY apply disaster civ-effects (civilization.rs:2533-2542); tick_modifiers (called post-loop at 884) already counts them down — so a disaster only needs to PUSH the modifier.

CivModifier (civilization.rs:478-486): { id, kind, label, polarity, remaining_turns, intensity }.

INSERTION SITE — advance_civ_turn (civilization.rs:782-900), exact lines:
```rust
snapshot.turn = next_turn;                  // line 794 (existing)
// ◄ INSERT: tick_environment(&mut snapshot);
let turn_order = civ_turn_order(&snapshot); // line 798 (existing) — civs now observe new env
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tick_environment orchestrator (locked sequence + logging) + insertion in advance_civ_turn (ENV-01/02/03)</name>
  <files>tauri-app/src-tauri/src/civilization.rs</files>
  <read_first>
    - tauri-app/src-tauri/src/civilization.rs (advance_civ_turn 782-900 esp. insertion at 794-798, resolve_environment 2504-2581, tick_modifiers 2586-2593, push_log 4450-4464, CivEnvironment 491-513, CivDisaster 515-523, CivModifier 478-486, CivSessionSnapshot fields 283-305, CivWorld 308-317, modifier_from_intervention construction shape 3876-3908)
    - .planning/phases/03-w4-environment-engine/03-PATTERNS.md (tick_environment section — mutator+log shape, countdown+retain shape, CONTEXT-locked sequence; Insertion site section; Shared Patterns → Logging + Disaster civ-effect via existing CivModifier kinds)
    - .planning/phases/03-w4-environment-engine/03-RESEARCH.md (System Architecture Diagram lines 99-139, Pattern 3, Open Q3 forecast-eta convention, Pitfall 5)
    - .planning/phases/03-w4-environment-engine/03-CONTEXT.md (Integration & Determinism — the locked sequence + turn-start placement)
    - 03-01-PLAN.md + 03-02-PLAN.md (the helper signatures + the remaining_turns-as-lead convention this orchestrator consumes)
  </read_first>
  <behavior>
    - Test (tick_environment_advances_season): calling tick_environment on a test_snapshot advances environment.turn_of_season (or season on wrap) and changes temperature; a "season" log entry exists after a wrap.
    - Test (forecast_then_fire): starting from a snapshot with a forecast whose remaining_turns==1, one tick_environment fires it — the disaster moves from env.forecast into env.disasters[] (with remaining_turns reset to its active duration) and a "disaster" log entry exists; a forecast with remaining_turns==3 only decrements (stays in env.forecast) and is NOT in disasters[] yet.
    - Test (disaster_logged): after a fire, the log contains a "disaster" entry; after a forecast roll, the log contains a "forecast" entry.
    - Test (disaster_expiry): a disaster in env.disasters[] with remaining_turns==1 is removed after a tick (countdown→retain), with an expiry log entry.
    - Test (fired_disaster_pushes_reused_modifier): a fired "drought"/"cold_snap" disaster pushes a CivModifier of that existing kind onto snapshot.modifiers (so the existing resolve_environment arm applies it).
    - Test (regrowth_runs_in_tick): a renewable tile below cap has higher amount after a summer tick; an ore tile is unchanged.
  </behavior>
  <action>
    Per CONTEXT "Integration & Determinism" (the locked sequence + turn-start) and RESEARCH System Architecture Diagram + Open Q3. Add the orchestrator `tick_environment(&mut snapshot)` (impure: mutates snapshot + logs) that calls the four Wave-1/2 helpers in the LOCKED order, then insert it in advance_civ_turn.

    1. Add `tick_environment(snapshot: &mut CivSessionSnapshot)` implementing this exact sequence (RESEARCH diagram lines 106-117):
       a. **Fire due forecast.** If `env.forecast` is `Some(d)`: decrement its lead via `d.remaining_turns = d.remaining_turns.saturating_sub(1)`. If it reaches 0 → take it out of `env.forecast`, RESET `d.remaining_turns` to its active duration (e.g. `disaster_duration(&d.kind)`, a small helper or a `.clamp`'d const like 3..6), call `apply_disaster_to_tiles(&mut snapshot.world.tiles, &d, snapshot.world.width)`, then for kinds with an existing civ-effect arm push a reused `CivModifier` (drought→`kind:"drought"`, cold_snap/ice→`kind:"cold_snap"`) with a seed/turn-derived id `format!("dis-{}-{}", snapshot.turn, d.kind)` (NOT uuid); `push_log(snapshot, "disaster", ...)`; finally push `d` into `snapshot.environment.disasters`. (storm/flood/quake/predator_incursion get NO lingering modifier — flood/quake already reshaped terrain in apply_disaster_to_tiles; storm/predator_incursion are announce/one-shot per Pitfall 5 — do NOT push an unknown modifier kind.)
       b. **Advance season/temp/water.** Call `advance_season(&env.season, env.turn_of_season, env.temperature, env.water_level, snapshot.turn, snapshot.seed)`; assign the returned tuple back to env; if the season string CHANGED, `push_log(snapshot, "season", ...)`.
       c. **Regrow resources.** `regrow_resources(&mut snapshot.world.tiles, &season, temperature)` using the just-advanced season/temperature.
       d. **Tick disaster countdowns + expiry.** Mirror `tick_modifiers` exactly on `snapshot.environment.disasters`: `for d in iter_mut() { d.remaining_turns = d.remaining_turns.saturating_sub(1); }`, log each that reaches 0 (`push_log(snapshot, "disaster", "... subsided", ...)` BEFORE retain — collect expiring ids first to avoid borrow conflicts), then `retain(|d| d.remaining_turns > 0)`.
       e. **Roll/refresh forecast.** If `env.forecast` is now `None`, call `roll_forecast(snapshot.seed, snapshot.turn, &env, snapshot.world.width)`; if `Some` → set `env.forecast` and `push_log(snapshot, "forecast", "A {kind} is forecast", ...)`.
       Sequence note: read fields into locals BEFORE mutating to avoid aliasing `&snapshot.environment` and `&mut snapshot.world.tiles`/`push_log(snapshot,...)` simultaneously (clone the small env scalars/forecast as needed; tiles are borrowed only inside the helper calls). Keep ALL randomness seed^turn-derived.
    2. Add a tiny `disaster_duration(kind:&str) -> u32` helper (or const) returning a small bounded active duration per kind (e.g. flood 4, drought 5, cold_snap 5, quake 2, storm 2, predator_incursion 3), each `.clamp(1, 12)`.
    3. **Insert the call** in `advance_civ_turn` exactly between line 794 and 798:
    ```rust
    snapshot.turn = next_turn;
    tick_environment(&mut snapshot);          // ◄ turn START — civs observe the new env this turn
    let turn_order = civ_turn_order(&snapshot);
    ```
    Do NOT place it at turn end and do NOT move the existing post-loop `resolve_environment`/`tick_modifiers` — the fired disaster's CivModifier deliberately rides them.
    4. Add the integration tests from `<behavior>` into `mod tests`, using `test_snapshot("t","T","mock",seed,1)`; for forecast tests set `snapshot.environment.forecast = Some(CivDisaster{...remaining_turns:1...})` then tick and assert the move + log; for season/regrowth assert env fields + tile amount deltas + the presence of a log entry of the right kind (`snapshot.log.iter().any(|e| e.kind == "season")`). Name them per `<behavior>`.
    DETERMINISM RULE (threat T-03-08): every id/roll in tick_environment derives from `seed`+`turn`; iterate `disasters`/`tiles` by Vec order (never HashMap); NO uuid/SystemTime. SEQUENCE RULE: the order is locked (fire→advance→regrow→countdown→roll) — do not reorder. PLACEMENT RULE: turn start only.
  </action>
  <verify>
    <automated>cd tauri-app/src-tauri && cargo test --no-run 2>&1 | tail -5 && cargo clippy --all-features -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - civilization.rs contains `fn tick_environment`
    - civilization.rs advance_civ_turn contains `tick_environment(&mut snapshot);` AND it appears after `snapshot.turn = next_turn;` and before `let turn_order = civ_turn_order(`
    - tick_environment body calls all four helpers: `advance_season`, `regrow_resources`, `roll_forecast`, `apply_disaster_to_tiles` (grep each)
    - tick_environment body contains `push_log(` with the kinds "season", "forecast", AND "disaster"
    - tick_environment body contains `.retain(` (disaster expiry mirror of tick_modifiers) and `saturating_sub` (countdown)
    - tick_environment body contains `format!("dis-` (seed/turn-derived modifier/disaster id) and does NOT contain `new_v4` or `SystemTime`
    - `cargo test --no-run` exits 0
    - `cargo clippy --all-features -- -D warnings` adds 0 new warnings over the 16-error baseline (.planning/phases/01-.../deferred-items.md)
    - mod tests contains the forecast_then_fire test AND a season-advance test AND a disaster-logged test
  </acceptance_criteria>
  <done>tick_environment runs the locked sequence (fire→advance→regrow→countdown→roll) once per turn at turn start, reshapes terrain on fire, reuses existing CivModifier kinds for civ-effects, logs every world event with distinct kinds, and is inserted at advance_civ_turn line 794; integration tests compile; clippy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: byte-determinism + back-compat integration tests for the full env tick (ENV-01/02/03)</name>
  <files>tauri-app/src-tauri/src/civilization.rs</files>
  <read_first>
    - tauri-app/src-tauri/src/civilization.rs (mod tests + test_snapshot 4918-4946, world_generation_is_deterministic_by_seed serde-compare pattern 4948-4964, legacy_v1_snapshot_migrates_to_multi_civ 5561-5613, default_environment + #[serde(default)] on environment 296-301 + 414, push_log 4450)
    - .planning/phases/03-w4-environment-engine/03-PATTERNS.md (Test Patterns — Pure-helper determinism + Integration test + Save back-compat test)
    - .planning/phases/03-w4-environment-engine/03-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Runtime State Inventory back-compat row)
    - .planning/phases/03-w4-environment-engine/03-VALIDATION.md (Per-Task Verification Map — the determinism + back-compat rows)
  </read_first>
  <behavior>
    - Test (tick_environment_deterministic): clone a test_snapshot, run tick_environment on BOTH clones, then assert serde_json::to_string(&a.environment) == serde_json::to_string(&b.environment) AND serde_json::to_string(&a.world.tiles) == serde_json::to_string(&b.world.tiles) (byte-deterministic for a given seed,turn).
    - Test (tick_environment_multi_turn_deterministic): run N (e.g. 12) sequential ticks (incrementing snapshot.turn each time as advance_civ_turn would) on two clones; assert final environment + world.tiles serde-equal (multi-turn replay stability — exercises a full forecast→fire→expiry cycle).
    - Test (tile_count_invariant_after_ticks): after N ticks, snapshot.world.tiles.len() == (width * WORLD_HEIGHT) (no tiles added/removed by any reshape).
    - Test (old_save_loads_calm_spring): a snapshot JSON missing the `environment` key (or with the legacy shape) deserializes with default_environment (calm spring) — extend the existing migration test pattern; confirm no panic and season=="spring".
  </behavior>
  <action>
    Per RESEARCH Validation Architecture (determinism is the load-bearing property) + Runtime State Inventory (back-compat is handled by existing #[serde(default)]). Add the cross-cutting integration tests that prove the WHOLE tick is deterministic and that old saves still load. These tests gate the phase (CI green before /gsd-verify-work). NO production code changes in this task — tests only.

    1. Add `tick_environment_deterministic`: build `let mut a = test_snapshot("det","Det","mock",777,1); let mut b = a.clone();` advance both turns identically (set both `.turn` to the same value), call `tick_environment(&mut a)` and `tick_environment(&mut b)`, then serde-compare `environment` and `world.tiles` (mirror `world_generation_is_deterministic_by_seed`'s serde-compare at 4954-4957).
    2. Add `tick_environment_multi_turn_deterministic`: loop `for t in 1..=12 { a.turn=t; tick_environment(&mut a); b.turn=t; tick_environment(&mut b); }` then serde-compare final `environment` + `world.tiles`. Pick a seed that triggers at least one disaster fire within 12 turns (try a couple seeds in the test; document the chosen seed in a comment) so the cycle is actually exercised.
    3. Add `tile_count_invariant_after_ticks`: capture `let n = a.world.tiles.len();` before the loop, assert `a.world.tiles.len() == n` and `== (a.world.width * WORLD_HEIGHT) as usize` after.
    4. Add `old_save_loads_calm_spring`: mirror `legacy_v1_snapshot_migrates_to_multi_civ` (5561-5613) — serialize a snapshot, strip the `environment` key from the JSON via serde_json::Value manipulation, re-parse through the normal load/parse path, assert it loads with `season == "spring"` and no panic. (This proves the no-new-field line held — if a field had been added without serde(default), this would fail.)
    BACK-COMPAT RULE (threat T-03-09): this phase added NO fields to CivEnvironment/CivDisaster/CivTile, so the existing `#[serde(default = "default_environment")]` (300) covers old saves. If any acceptance grep below fails because a field WAS added, STOP and either revert the field or add the export_bindings+tsc task (gotcha #1) — but the intended outcome is no field change and no bindings regen.
  </action>
  <verify>
    <automated>cd tauri-app/src-tauri && cargo test --no-run 2>&1 | tail -5 && cargo clippy --all-features -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mod tests contains `fn tick_environment_deterministic` AND `fn tick_environment_multi_turn_deterministic`
    - mod tests contains `fn tile_count_invariant_after_ticks` asserting `width * WORLD_HEIGHT`
    - mod tests contains `fn old_save_loads_calm_spring`
    - the determinism tests use `serde_json::to_string(` to byte-compare environment AND world.tiles
    - civilization.rs CivEnvironment struct field set is UNCHANGED (still exactly: season, turn_of_season, temperature, water_level, disasters, forecast) AND CivDisaster field set is UNCHANGED (id, kind, epicenter_x, radius, intensity, remaining_turns) — confirming no bindings regen needed
    - tauri-app/src/bindings.ts is unchanged by this phase (no `#[derive(Type)]` field added)
    - `cargo test --no-run` exits 0
    - `cargo clippy --all-features -- -D warnings` adds 0 new warnings over the 16-error baseline
  </acceptance_criteria>
  <done>The full env tick is proven byte-deterministic over single and multi-turn runs (serde-compare of environment + world.tiles), the tile-count invariant holds after many ticks including disaster fires, and old saves still load as calm spring — confirming the no-new-field / no-bindings-regen line held; tests compile; clippy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Per-turn env tick → snapshot.environment + world.tiles + log | tick_environment mutates the canonical world state every turn; correctness/determinism of this mutation is the integrity boundary (replay/eval integrity). |
| Persisted snapshot JSON → engine deserialize (load & re-save each turn) | Each turn loads, mutates, and re-saves the snapshot; old saves (pre-env-logic or hand-edited) must still load, and a given (seed,turn) must serialize identically across runs. |
| No external/network/auth surface | tick_environment reads only seed/turn/world; no user input, no provider keys. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering / Repudiation (deterministic-replay & eval integrity) | `tick_environment` full sequence | mitigate | Every id/roll derives from seed+turn (via the Wave-1/2 helpers + `format!("dis-{turn}-{kind}")`); disasters/tiles iterated by Vec order, never HashMap; NO uuid/SystemTime in the tick. Asserted by `tick_environment_deterministic` + `tick_environment_multi_turn_deterministic` (serde byte-compare of environment + world.tiles). |
| T-03-09 | Tampering (persisted-snapshot back-compat) | save format / no new fields | mitigate | This phase adds NO fields to CivEnvironment/CivDisaster/CivTile; existing `#[serde(default = "default_environment")]` keeps old saves loading as calm spring. `old_save_loads_calm_spring` test + the unchanged-field-set acceptance grep enforce the line; if violated, executor must add export_bindings+tsc (gotcha #1) — but intended outcome is no regen. |
| T-03-10 | Denial of Service (corrupted-world / soft-brick) | terrain reshape over many turns | mitigate | apply_disaster_to_tiles (03-02) is bounded + in-place; this plan adds the per-turn invariant test `tile_count_invariant_after_ticks` to prove repeated fires never add/remove tiles or escape `width * WORLD_HEIGHT`. saturating arithmetic on all countdowns. |
| T-03-11 | Tampering | new CivModifier kind with no resolve_environment arm (silent no-op) | mitigate | Only drought/cold_snap (which have arms) are pushed by a fired disaster; flood/quake are terrain-only; storm/predator_incursion are announce/one-shot. No unknown modifier kind introduced (Pitfall 5). |
</threat_model>

<verification>
- `cargo test --no-run` compiles all tests on Windows; full `cargo test` runs the new integration + determinism tests on CI (Linux/macOS) — backend tests cannot run on Windows (gotcha #5). This is the phase gate (CI green before /gsd-verify-work).
- `cargo clippy --all-features -- -D warnings` adds ZERO new warnings over the documented 16-error baseline (do NOT fix the baseline).
- bindings.ts is UNCHANGED — no `#[derive(Type)]` field added, no `#[specta::specta]` command added (CivEnvironment/CivDisaster field sets unchanged); no `cargo run --bin export_bindings`, no `tsc` needed. (If a field were unavoidably added, the executor MUST run `cargo run --bin export_bindings` + `npx tsc --noEmit` per gotcha #1 — but the intended and asserted outcome is no field change.)
- tauriBrowserFallback.ts is intentionally NOT mirrored — advancePreviewCiv is a cosmetic stub (RESEARCH A1 / State of the Art); no `.ts` change this phase.
- Determinism is the load-bearing automated property: single + multi-turn ticks serde-compare identical; tile count invariant holds; old saves load.
</verification>

<success_criteria>
- `tick_environment` runs once per turn at turn start (advance_civ_turn line 794) executing the locked sequence; civs observe the freshly-advanced season/forecast/disaster the same turn.
- Seasons drift temperature, disasters forecast→fire→reshape terrain (logged), renewables regrow while finite stays depleted — end to end (ENV-01/02/03) with zero frontend changes (the frontend/harness already consume environment).
- Every forecast/fire/season-change/expiry is logged with a distinct kind.
- The full tick is byte-deterministic (single + multi-turn), the tile-count invariant holds across many ticks, and old saves still load as calm spring.
- `cargo test --no-run` exits 0; clippy adds no new warnings; bindings.ts unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/03-w4-environment-engine/03-03-SUMMARY.md`
</output>
