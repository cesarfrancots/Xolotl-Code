---
phase: 01-w9-lite-multi-model-world-creation-leaderboard
plan: 03
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tauri-app/src/components/civilization/CivilizationGameCanvas.tsx
  - tauri-app/src/lib/civPilot.ts
  - tauri-app/scripts/codex-play-civ.mjs
  - tauri-app/src/lib/civPilot.test.ts
autonomous: true
requirements: [ARENA-01, ARENA-02]
must_haves:
  truths:
    - "render_game_to_text() output includes civs[] (per-civ summaries) + leaderboard + environment (D-13)"
    - "render_game_to_text() output keeps civilization.resources, player, player_task, visible_entities byte-identical (legacy harness unbroken) (D-13)"
    - "codex-play-civ.mjs still parses state.civilization.resources and drives via the existing control surface (D-13)"
    - "CivPilotTextState type additively models civs[] / leaderboard / environment as structured JSON (D-14)"
  artifacts:
    - path: "tauri-app/src/components/civilization/CivilizationGameCanvas.tsx"
      provides: "Additive render_game_to_text extension (civs[] + leaderboard + environment)"
      contains: "leaderboard"
    - path: "tauri-app/src/lib/civPilot.ts"
      provides: "CivPilotTextState additive fields (civs?, leaderboard?, environment?)"
      contains: "leaderboard"
    - path: "tauri-app/src/lib/civPilot.test.ts"
      provides: "Contract test: legacy keys preserved + new keys present"
      contains: "civilization"
  key_links:
    - from: "renderSnapshotToText"
      to: "window.render_game_to_text"
      via: "existing hook (CivilizationGameCanvas.tsx:255/301)"
      pattern: "render_game_to_text"
    - from: "render_game_to_text output"
      to: "codex-play-civ.mjs"
      via: "JSON.parse, reads state.civilization.resources (additive-safe)"
      pattern: "civilization.resources"
---

<objective>
Extend the arena bridge read API additively (ARENA-01) without breaking the legacy harness (ARENA-02). Append `civs[]` (per-civ summaries), `leaderboard`, and `environment` to `render_game_to_text()` while keeping `session`/`civilization`/`player`/`player_task`/`visible_entities` byte-identical, extend the `CivPilotTextState` type to model the new keys, and lock the contract with a vitest spec. `codex-play-civ.mjs` keeps working unchanged (and optionally reads the new keys).

Purpose: Lets an external/agentic harness read the full multi-civ state + leaderboard as structured JSON (no pixel parsing) while the existing single-civ harness contract stays intact — the hard back-compat gate.

Output: Updated `CivilizationGameCanvas.tsx` + `civPilot.ts` + `codex-play-civ.mjs` (optional read) and an extended `civPilot.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-RESEARCH.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md
@.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Verified anchors 2026-06-06. -->

renderSnapshotToText(snapshot, playerState?) — CivilizationGameCanvas.tsx:~3135. Returns a single JSON.stringify of:
{ coordinate_system, session{id,turn,model}, civilization{...primaryCiv...}, player{...}, player_task, visible_entities[] }
Wired to window.render_game_to_text at 255 & 301; Phaser-scene renderToText() fallback at ~594 also calls it.

Legacy consumer contract that MUST keep working (codex-play-civ.mjs):
- readState (148-151): JSON.parse(window.render_game_to_text())
- reads state.player.player (155), state.player_task (159), state.visible_entities (191-194, 208), state.civilization.resources (177)

CivPilotTextState (civPilot.ts:60-97) — all-optional (?:) structural type modeling session/player/player_task/visible_entities.

snapshot.civs each carry: id, name, model, color, alive, population, era, score{survival,ethics,intelligence,total}, controller (Plan 01), resources. snapshot.environment is already normalized (civStore.ts:148-158).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive render_game_to_text (civs[] + leaderboard + environment) + CivPilotTextState type (ARENA-01)</name>
  <files>tauri-app/src/components/civilization/CivilizationGameCanvas.tsx, tauri-app/src/lib/civPilot.ts</files>
  <read_first>
    - tauri-app/src/components/civilization/CivilizationGameCanvas.tsx (renderSnapshotToText ~3135, window.render_game_to_text hooks 255/301, scene renderToText fallback ~594)
    - tauri-app/src/lib/civPilot.ts (CivPilotTextState 60-97)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md (render_game_to_text additive + civPilot.ts sections, Pitfall 3)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-RESEARCH.md (section 5)
  </read_first>
  <behavior>
    - Test: render output still contains civilization.resources, player, player_task, visible_entities (legacy keys, byte-identical structure).
    - Test: render output now contains civs[] with {id,name,model,color,alive,population,era,score,controller,resources}.
    - Test: render output now contains a leaderboard array sorted by score.total desc and an environment key.
  </behavior>
  <action>
    Per D-13 (additive only), D-14 (structured JSON, single stringify), ARENA-01. Pitfall 3: do NOT rename/remove existing keys.

    1. In `renderSnapshotToText` (~3135), keep `session`, `civilization` (still `primaryCiv(...)` — the legacy harness reads civilization.resources, codex-play-civ.mjs:177), `player`, `player_task`, `visible_entities` byte-identical. APPEND inside the same single JSON.stringify object:
    ```js
    civs: snapshot.civs.map(c => ({
      id: c.id, name: c.name, model: c.model, color: c.color,
      alive: c.alive, population: c.population, era: c.era, score: c.score,
      controller: c.controller ?? null, resources: c.resources,
    })),
    leaderboard: [...snapshot.civs].sort((a, b) => b.score.total - a.score.total)
      .map(c => ({ id: c.id, name: c.name, model: c.model, color: c.color,
        alive: c.alive, score: c.score, controller: c.controller ?? null })),
    environment: snapshot.environment,
    ```
    The civs[] field set satisfies the discretion list (id, name, model, color, alive, population, era, score, controller). Do not add a second JSON.stringify — append keys to the existing object (D-14).
    2. Extend `CivPilotTextState` (civPilot.ts:60-97) additively (all optional, nothing existing changed):
    ```ts
    civs?: Array<{ id: string; name: string; model: string; color: string;
      alive: boolean; population: number; era: string;
      score: { survival: number; ethics: number; intelligence: number; total: number };
      controller: string | null; resources: Record<string, number> }>;
    leaderboard?: Array<{ id: string; name: string; model: string; color: string;
      alive: boolean; score: { survival: number; ethics: number; intelligence: number; total: number }; controller: string | null }>;
    environment?: unknown;
    ```
    3. SECURITY (threat T-03-01): the appended fields carry only civ identity/score/resources/controller — verify none of them sources from the provider config map. controller is the sanitized label from Plan 01. Do not serialize any key/secret material into the text-state.
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | tail -5 && npm test -- civPilot 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CivilizationGameCanvas.tsx renderSnapshotToText output object contains `civs:`, `leaderboard:`, and `environment:` keys
    - CivilizationGameCanvas.tsx still contains `civilization:` (primaryCiv) — legacy key preserved
    - Only one JSON.stringify in renderSnapshotToText (grep count of JSON.stringify in the function unchanged at 1)
    - civPilot.ts CivPilotTextState contains `civs?` AND `leaderboard?` AND `environment?`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>render_game_to_text additively exposes civs[]/leaderboard/environment while preserving the legacy keys; CivPilotTextState models them; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Contract test (legacy keys preserved + new keys present) + codex harness optional read (ARENA-02)</name>
  <files>tauri-app/src/lib/civPilot.test.ts, tauri-app/scripts/codex-play-civ.mjs</files>
  <read_first>
    - tauri-app/src/lib/civPilot.test.ts (existing spec structure)
    - tauri-app/src/components/civilization/CivilizationGameCanvas.tsx (renderSnapshotToText after Task 1)
    - tauri-app/scripts/codex-play-civ.mjs (readState 148-152, progressSignature/civilization.resources 177, controls.start ~869)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-PATTERNS.md (codex-play-civ.mjs section)
    - .planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-VALIDATION.md (Back-Compat Regression Checks)
  </read_first>
  <action>
    Per ARENA-02 (no broken controls) and the VALIDATION back-compat gate.

    1. Extend `civPilot.test.ts` with a contract spec on the render output: build a multi-civ snapshot fixture (2-3 civs), call the render function, parse it, and assert BOTH:
       - legacy keys present and well-formed: `state.civilization.resources` (object), `state.player`, `state.player_task`, `state.visible_entities` (array) — this is the codex-play-civ.mjs contract (Pitfall 3 / VALIDATION row "codex-play-civ.mjs still parses text-state").
       - new keys present: `state.civs` (length === fixture civ count, each with id/name/model/color/alive/score/controller), `state.leaderboard` (sorted by score.total desc), `state.environment` defined.
    2. codex-play-civ.mjs: this is additive-safe and requires NO change for ARENA-02 back-compat. OPTIONALLY read the new keys (`state.leaderboard` / `state.civs`) for richer logging while leaving `playerOf`/`taskOf`/`state.civilization.resources`/`state.visible_entities` reads intact. Do NOT alter the existing `controls.start({goal, possessId, requesterId, continueAfterTask})` call shape (~869) — the civId/controller driving wiring lands in Plan 04 (CivilizationView.tsx) and remains backward compatible.
  </action>
  <verify>
    <automated>cd tauri-app && npm test -- civPilot 2>&1 | tail -20 && node --check scripts/codex-play-civ.mjs 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - civPilot.test.ts asserts `civilization.resources` AND `player` AND `visible_entities` present (legacy)
    - civPilot.test.ts asserts `civs` AND `leaderboard` AND `environment` present (new)
    - `npm test -- civPilot` passes
    - `node --check scripts/codex-play-civ.mjs` exits 0 (if the file was edited)
    - codex-play-civ.mjs still reads `state.civilization.resources` (grep match)
  </acceptance_criteria>
  <done>A contract test locks both the legacy keys and the new additive keys; the codex harness remains parseable and its control-call shape unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engine snapshot -> render_game_to_text -> external harness | The text-state is consumed/driven by an external agentic harness (codex-play-civ.mjs); it is the integration surface, treated as untrusted both directions. |
| Provider config map | Holds API keys (gotcha #2); must never appear in the text-state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | render_game_to_text output | mitigate | Appended fields carry only civ identity/score/resources/controller (a sanitized label, Plan 01); no config-map/key material is serialized. Verified by review of the appended object (Task 1). |
| T-03-02 | Tampering | external harness consuming text-state | accept | Harness reads structured JSON only (no eval, no pixel parsing, D-14). The bridge is read-only here; driving lands via the existing control surface (Plan 04). Local single-user desktop context. |
| T-03-03 | Spoofing | controller tag shown in leaderboard text-state | mitigate | controller is sanitized at the source (Plan 01 T-01-02: trim + 64-char cap); the text-state passes it through unmodified. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean against the regenerated bindings (Plan 01).
- `npm test -- civPilot` green (frontend runs on Windows).
- Back-compat (VALIDATION hard gate): contract test asserts civilization.resources/player/visible_entities preserved AND `node --check` passes for the harness script.
</verification>

<success_criteria>
- render_game_to_text() exposes civs[] + leaderboard + environment additively (ARENA-01) with legacy keys byte-identical (ARENA-02).
- A vitest contract test locks both old and new keys.
- codex-play-civ.mjs remains parseable with its control-call shape unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/01-w9-lite-multi-model-world-creation-leaderboard/01-03-SUMMARY.md`
</output>
