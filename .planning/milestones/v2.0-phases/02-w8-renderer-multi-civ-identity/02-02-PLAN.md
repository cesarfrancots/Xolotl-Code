---
phase: 02-w8-renderer-multi-civ-identity
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - tauri-app/src/components/civilization/CivilizationGameCanvas.tsx
  - tauri-app/src/components/civilization/civCanvas.test.ts
  - tauri-app/src/components/civilization/CivilizationView.tsx
  - tauri-app/src/components/civilization/CivilizationView.test.tsx
autonomous: true
requirements: [REN-02]
must_haves:
  truths:
    - "The camera frames ALL living colonies by default (bounding box over living civs) on world create/load and re-frames on civ collapse, dropping dead civs (REN-02, D-frame)"
    - "window.civCamera.focusCiv(civId) pans+zooms to that civ's colony and frameAll() resets to the all-colonies view; both are ADDED additively — zoomBy/recenter/toggleFollow/focusRegion remain (REN-02, ARENA-02 extend-only)"
    - "Selecting a civ (selectedCivId, e.g. a leaderboard row click) drives window.civCamera.focusCiv; clearing selection (null) calls frameAll (REN-02, D-focus)"
    - "colonyBounds and focusTarget are pure named exports unit-tested under vi.mock(\"phaser\"); the additive bridge contract is asserted by a component test"
  artifacts:
    - path: "tauri-app/src/components/civilization/CivilizationGameCanvas.tsx"
      provides: "colonyBounds/focusTarget pure exports; recomputeColonies + frameAll multi-colony fit; additive focusCiv/frameAll on window.civCamera; both Window typedefs extended"
      contains: "focusCiv"
    - path: "tauri-app/src/components/civilization/CivilizationView.tsx"
      provides: "useEffect([selectedCivId]) → window.civCamera.focusCiv / frameAll wiring; Window typedef mirror"
      contains: "focusCiv"
    - path: "tauri-app/src/components/civilization/CivilizationView.test.tsx"
      provides: "Additive-bridge contract + selectedCivId→focusCiv component assertions"
      contains: "focusCiv"
  key_links:
    - from: "onResize !framed / civ collapse"
      to: "frameAll() via colonyBounds(this.colonies, pad)"
      via: "recomputeColonies builds one point per living civ"
      pattern: "colonyBounds"
    - from: "window.civCamera.focusCiv(civId)"
      to: "cam.pan + cam.zoomTo at focusTarget(...)"
      via: "additive bridge in installCameraApi (existing 4 methods kept)"
      pattern: "focusCiv"
    - from: "CivilizationView useEffect([selectedCivId])"
      to: "window.civCamera?.focusCiv(selectedCivId) / frameAll()"
      via: "selectedCivId store signal (Phase 1) → leaderboard row click"
      pattern: "civCamera\\?\\.focusCiv"
---

<objective>
Frame the multi-civ world (REN-02). Extract the pure camera helpers (`colonyBounds`, `focusTarget`) as NAMED EXPORTS and unit-test them, generalize the single-`this.colony` framing into a multi-colony bounding box (`recomputeColonies` + `frameAll`), add `focusCiv(civId)` and `frameAll()` ADDITIVELY to `window.civCamera` (keeping the existing four methods), extend BOTH hand-written `Window.civCamera` typedefs, and wire `selectedCivId` → `focusCiv` in `CivilizationView.tsx` so a leaderboard row click focuses that civ. Keep rendering performant at the larger multi-civ scale (re-tint/re-frame only on change, not per frame).

Purpose: REN-02 — the camera must show all colonies by default and let the user focus one. This consumes the `civColorById`/civ-identity data from Plan 01 and the Phase 1 `selectedCivId` selection signal, completing the renderer multi-civ identity phase.

Output: Updated `CivilizationGameCanvas.tsx` (camera helpers + framing + additive bridge + typedef); updated `CivilizationView.tsx` (wiring + typedef mirror); extended `civCanvas.test.ts` (camera unit rows) and `CivilizationView.test.tsx` (additive-contract + focus component test). No backend, no `bindings.ts` regen (no IPC change — only the two hand-written typedefs change).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-w8-renderer-multi-civ-identity/02-CONTEXT.md
@.planning/phases/02-w8-renderer-multi-civ-identity/02-RESEARCH.md
@.planning/phases/02-w8-renderer-multi-civ-identity/02-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Verified anchors 2026-06-07 against the live files. depends_on Plan 01: civColorById + the pure-helper export pattern + civCanvas.test.ts already exist. -->

CivilizationGameCanvas.tsx:
- `private colony = { x: 0, y: 0 }` 433 (KEEP as follow fallback; add `private colonies` + `private civColorById`[from Plan 01] next to it).
- `recomputeColony()` 2968-2983 — single-point pick (pond→nest→axo centroid→world center). Add a sibling `recomputeColonies()`.
- `onResize()` 2947-2965 — fit-on-first-frame at 2957-2964: `if (!this.framed) { this.framed = true; cam.setZoom(...); cam.centerOn(this.colony.x, this.colony.y); }`. Generalize the !framed branch to fit colonyBounds().
- `this.framed` flips false in rebuildWorld at 657 (world create/load); `this.following` 567 (manual drag sets false at ~1701; focusRegion sets false at 2936).
- `installCameraApi()` 2917-2945 → `window.civCamera = { zoomBy, recenter, toggleFollow, focusRegion }` at 2918. focusRegion (2935-2943) is the pan+zoom math to mirror:
  ```typescript
  focusRegion: (rx, width) => {
    this.following = false;
    const cx = (rx + width / 2) * TILE_SIZE; const cy = this.worldH * 0.46;
    const cam = this.cameras.main;
    const target = Phaser.Math.Clamp(cam.width / (width * TILE_SIZE * 1.15), this.minZoom, this.maxZoom);
    cam.pan(cx, cy, 420, Phaser.Math.Easing.Sine.Out);
    cam.zoomTo(target, 420, Phaser.Math.Easing.Sine.Out);
  }
  ```
- Window.civCamera typedef 12-17 (4 methods) — add optional focusCiv?/frameAll?.
- step() yields to cam.panEffect.isRunning (~1085) — use cam.pan/zoomTo, do NOT hand-roll a per-frame lerp.

CivilizationView.tsx:
- selectedCivId consumer: `const selectedCivId = useCivStore((s) => s.selectedCivId);` (200).
- Camera-button bridge usage 1313-1321 / 2051: `onClick={() => window.civCamera?.zoomBy(1.2)}` etc. (the additive contract these must keep working).
- Window.civCamera typedef 62-67 (mirror of the canvas decl — keep in sync).

Data model (bindings.ts): CivCivilization.{id?,spawn_x?,home_region?,alive?}; CivRegion.{id,x,width,height?,owner?}; CivEntity.{civ_id?,x,y}.

Test analog — CivilizationView.test.tsx exists (334 lines): vi.mock at 12/17/29; leaderboard rows at 147/212/217/236/248; selectedCivId at 43/198/204/252/256/330. Extend it; do NOT rewrite.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure camera helpers (colonyBounds, focusTarget) + civCanvas.test.ts extension (REN-02 unit rows)</name>
  <files>tauri-app/src/components/civilization/CivilizationGameCanvas.tsx, tauri-app/src/components/civilization/civCanvas.test.ts</files>
  <read_first>
    - tauri-app/src/components/civilization/CivilizationGameCanvas.tsx (shade() 3089-3094 pure-helper shape; recomputeColony 2968-2983 for the single-point precedent generalized here; the Plan-01 helper exports to sit beside)
    - tauri-app/src/components/civilization/civCanvas.test.ts (the suite Plan 01 created — extend it; vi.mock("phaser") already at top)
    - .planning/phases/02-w8-renderer-multi-civ-identity/02-RESEARCH.md (Pattern 4 colonyBounds lines 224-236; Pattern 5 focusTarget lines 259-264; Test Map REN-02 unit rows 488-490; Assumption A2 line 431)
    - .planning/phases/02-w8-renderer-multi-civ-identity/02-PATTERNS.md (multi-colony camera fit 201-233; focusCiv bridge 235-266)
    - .planning/phases/02-w8-renderer-multi-civ-identity/02-VALIDATION.md (REN-02 unit rows)
  </read_first>
  <behavior>
    - Test: colonyBounds([{x:10,y:10,alive:true},{x:30,y:50,alive:true}], 0) → {x:10,y:10,w:20,h:40}; with pad it inflates by pad on each side.
    - Test: colonyBounds excludes alive:false colonies; returns null when zero living colonies (the collapse / no-colony case).
    - Test: focusTarget precedence — owned/home-region center when the civ's home_region resolves in regions; else the centroid of that civ's entities; else its spawn_x; else null when nothing resolves (RESEARCH Pattern 5 + A2 fallback).
  </behavior>
  <action>
    Per D-frame (bounding box over all living colonies) and D-focus (focusCiv resolves a civ's home point). Add PURE, Phaser-FREE named exports beside the Plan-01 helpers:

    1. `export function colonyBounds(colonies: {x:number;y:number;alive:boolean}[], pad: number): {x:number;y:number;w:number;h:number} | null` — filter to `alive`; return `null` when none; else min/max over x/y inflated by `pad` (RESEARCH lines 224-236). No `Phaser.*`.
    2. `export function focusTarget(civId, civs, regions, entities): {tx:number;ty:number} | null` — resolve in precedence order: (a) the civ's `home_region` matched in `regions` → that region's center in WORLD pixels (`(x + width/2)`, vertical center from height or a world-fraction constant); (b) else the centroid of `entities` whose `civ_id === civId`; (c) else the civ's `spawn_x` (with a sensible y); (d) else `null` (RESEARCH Pattern 5 lines 259-264, A2). Keep it pure: callers multiply by TILE_SIZE / apply easing — but if the helper returns world-pixel coords, document the unit clearly and test in those units. NO `Phaser.*`.
    3. Extend `civCanvas.test.ts` (created in Plan 01): import `colonyBounds`, `focusTarget` and add the <behavior> cases above. vi.mock("phaser") already sits at the top from Plan 01 — reuse it.
    4. Do NOT add Phaser to the helper bodies. Do NOT change IPC/bindings/backend.
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run civCanvas 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function colonyBounds" tauri-app/src/components/civilization/CivilizationGameCanvas.tsx` matches
    - `grep -n "export function focusTarget" tauri-app/src/components/civilization/CivilizationGameCanvas.tsx` matches
    - colonyBounds + focusTarget bodies contain NO `Phaser.` reference
    - civCanvas.test.ts imports colonyBounds and focusTarget and asserts: rect math, dead-colony exclusion, null-when-none, focusTarget precedence (home→centroid→spawn_x→null)
    - `npx tsc --noEmit` exits 0; `npx vitest run civCanvas` passes
  </acceptance_criteria>
  <done>colonyBounds and focusTarget are pure named exports covering the REN-02 unit rows (rect over living colonies, null when none, focus precedence) green; tsc 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-colony framing + additive focusCiv/frameAll bridge + both typedefs + selectedCivId wiring + component test (REN-02)</name>
  <files>tauri-app/src/components/civilization/CivilizationGameCanvas.tsx, tauri-app/src/components/civilization/CivilizationView.tsx, tauri-app/src/components/civilization/CivilizationView.test.tsx</files>
  <read_first>
    - tauri-app/src/components/civilization/CivilizationGameCanvas.tsx (colony 433; recomputeColony 2968-2983; onResize fit 2957-2964; installCameraApi + window.civCamera 2917-2945 with focusRegion math 2935-2943; Window typedef 12-17; this.framed 657; this.following 567; step panEffect ~1085)
    - tauri-app/src/components/civilization/CivilizationView.tsx (selectedCivId 200; camera-button bridge 1313-1321/2051; Window typedef 62-67)
    - tauri-app/src/components/civilization/CivilizationView.test.tsx (existing vi.mock 12/17/29; leaderboard rows 147/212/217/236/248; selectedCivId 198/204/252/256 — EXTEND, do not rewrite)
    - .planning/phases/02-w8-renderer-multi-civ-identity/02-PATTERNS.md (multi-colony camera fit 201-233; additive bridge 235-266; both typedefs 269-287; selectedCivId→camera wiring 290-306; Shared Patterns — extend-only bridge + camera-via-built-in-effects)
    - .planning/phases/02-w8-renderer-multi-civ-identity/02-RESEARCH.md (Pattern 4 frameAll 239-248; Pattern 5 bridge 266-274; Pitfall 3 re-framing 315-324; Anti-Pattern "replacing civCamera" 278; Runtime State line 409 — both typedefs hand-written, NO bindings regen)
    - CLAUDE.md (gotcha 1 — bindings.ts auto-generated; this phase makes NO IPC change, so do NOT regen; only the two Window.civCamera typedefs change)
  </read_first>
  <behavior>
    - Test (component, CivilizationView.test.tsx): after mount, window.civCamera retains zoomBy AND recenter AND toggleFollow AND focusRegion (the four must still be functions) AND now also has focusCiv AND frameAll (additive ARENA-02 contract).
    - Test (component): setting selectedCivId (or clicking the leaderboard row that sets it) invokes window.civCamera.focusCiv with that civ id; clearing selection to null invokes frameAll. Spy on window.civCamera.
  </behavior>
  <action>
    Per D-frame (default frames all living colonies; re-frame on create/load + collapse), D-focus (focusCiv pans+zooms to a civ; frameAll resets; wire to selectedCivId), and ARENA-02 extend-only (NEVER remove the four existing bridge methods).

    1. Multi-colony state: declare `private colonies: {civId:string;x:number;y:number;alive:boolean}[] = []` next to `private colony` (433). Add `private recomputeColonies()` (sibling to recomputeColony 2968-2983) that builds one point per LIVING civ from its home-region center → entities centroid → spawn_x (reuse the pure `focusTarget` logic where practical, or mirror it). Call it in setSnapshot (the same place Plan 01 added the civColorById build), and re-frame on collapse: when the set of living civs shrinks since last snapshot, call `frameAll()`.
    2. `private frameAll()`: `const b = colonyBounds(this.colonies, 6 * TILE_SIZE)`; if `!b` fall back to `cam.centerOn(this.colony.x, this.colony.y)`; else set `this.following = false`, compute `fit = Math.min(cw/b.w, ch/b.h)`, `cam.zoomTo(Phaser.Math.Clamp(fit, this.minZoom, this.maxZoom), 420, Sine.Out)`, `cam.pan(b.x+b.w/2, b.y+b.h/2, 420, Sine.Out)` (RESEARCH Pattern 4 lines 239-248). Use built-in pan/zoom — step already yields to cam.panEffect (Pitfall 3); do NOT lerp in step.
    3. `private focusCiv(civId: string)`: resolve `const tgt = focusTarget(civId, this.snapshot.civs, this.snapshot.world.regions, this.snapshot.world.entities)`; if null, fall back to frameAll(); else set `this.following = false` and `cam.pan(tgt.tx, tgt.ty, ...)` + `cam.zoomTo(clampedTarget, ...)` mirroring focusRegion (2935-2943). Convert helper units to world pixels consistently with focusRegion.
    4. onResize generalization (2957-2964): in the `!this.framed` branch, fit to `colonyBounds(this.colonies, pad)` over living colonies instead of `this.colony` (fall back to `this.colony` centerOn when bounds is null). Keep the `else` branch (zoom clamp) unchanged. Re-frame only on create/load (`framed` flips at 657) + collapse — NOT every snapshot (Pitfall 3).
    5. Additive bridge (installCameraApi 2917-2945): ADD `focusCiv: (civId) => this.focusCiv(civId)` and `frameAll: () => this.frameAll()` to the existing object literal. Do NOT remove or rename zoomBy/recenter/toggleFollow/focusRegion (ARENA-02 / Anti-Pattern RESEARCH 278).
    6. Typedefs (BOTH, keep in sync): add `focusCiv?(civId: string): void;` and `frameAll?(): void;` to the Window.civCamera decl in CivilizationGameCanvas.tsx (12-17) AND CivilizationView.tsx (62-67). Hand-written ambient typedefs — NO bindings.ts regen (no IPC surface change; CLAUDE.md gotcha 1 / RESEARCH line 409).
    7. Wiring (CivilizationView.tsx): add `useEffect(() => { if (selectedCivId) window.civCamera?.focusCiv(selectedCivId); else window.civCamera?.frameAll(); }, [selectedCivId])` (RESEARCH Pattern 5 wiring 273-274). The leaderboard-row → setSelectedCivId link already landed in Phase 1 (existing test ~243-253), so a row click flows through this effect.
    8. Component test (extend CivilizationView.test.tsx, do not rewrite): assert the additive-bridge contract (all six methods present after mount) and that selectedCivId change drives focusCiv / null drives frameAll. Spy on window.civCamera (mirror the ARENA-02 back-compat assertion style from Phase 1).
    9. Discretion (do NOT block on these): easing durations, the colonyBounds pad, and whether to dim non-selected civs while focused — implement frameAll+focusCiv first; add dimming only if it clearly helps (cheap single-alpha, never per-sprite). Do NOT regen bindings; do NOT touch backend or tauriBrowserFallback.ts.
  </action>
  <verify>
    <automated>cd tauri-app && npx tsc --noEmit 2>&1 | tail -5 && npx vitest run civCanvas 2>&1 | tail -6 && npx vitest run CivilizationView 2>&1 | tail -20 && npm test 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "private frameAll" AND "private focusCiv" AND "recomputeColonies"` all match in CivilizationGameCanvas.tsx
    - window.civCamera object literal in installCameraApi contains `focusCiv:` AND `frameAll:` AND still contains zoomBy/recenter/toggleFollow/focusRegion (grep all six)
    - Window.civCamera typedef in BOTH CivilizationGameCanvas.tsx (12-17) AND CivilizationView.tsx (62-67) contains `focusCiv?` AND `frameAll?`
    - CivilizationView.tsx contains a useEffect over `[selectedCivId]` calling `window.civCamera?.focusCiv` and `window.civCamera?.frameAll`
    - onResize !framed branch references `colonyBounds(` (multi-colony fit)
    - NO bindings change: `git diff --name-only` does NOT list `tauri-app/src/bindings.ts`
    - CivilizationView.test.tsx asserts all six bridge methods present (additive contract) AND selectedCivId→focusCiv / null→frameAll
    - `npx tsc --noEmit` exits 0; `npx vitest run CivilizationView` + `npx vitest run civCanvas` pass; full `npm test` stays green (≥214)
  </acceptance_criteria>
  <done>Default view frames all living colonies and re-frames on collapse; window.civCamera additively gains focusCiv+frameAll (four existing methods intact); both typedefs extended without a bindings regen; selectedCivId drives focus / null resets; component + unit tests green; tsc 0 and full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external harness / UI → window.civCamera bridge | UI camera buttons and an external harness (ARENA-02) call the extend-only bridge; this plan adds focusCiv/frameAll that they may invoke. |
| snapshot civs/regions/entities (backend) → camera target math | civ_id / home_region / spawn_x / region geometry cross into focusTarget; only ever become camera coordinates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | focusTarget / focusCiv with a missing or malformed civ (no region/entities/spawn_x) | mitigate | focusTarget returns `null` for an unresolvable civ and focusCiv falls back to frameAll(); colonyBounds returns `null` when no living colony (falls back to this.colony centerOn). No throw on bad/empty input — unit-tested (Task 1). |
| T-02-05 | Tampering | replacing the window.civCamera object would break zoomBy/recenter/focusRegion already used by the camera buttons (ARENA-02) | mitigate | The bridge is EXTENDED, never replaced — focusCiv/frameAll are added keys; a component test asserts all six methods remain (additive contract). |
| T-02-06 | Tampering / XSS | selectedCivId value used as a focus argument | accept (N/A) | selectedCivId is an internal store id passed only as a camera-target lookup key; it is never rendered as HTML and never reaches a DOM sink. Phaser draws to WebGL. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run civCanvas` green (camera unit rows: colonyBounds, focusTarget).
- `npx vitest run CivilizationView` green (additive-bridge contract + selectedCivId→focusCiv).
- Full `npm test` stays green (currently 214; must not regress).
- Additive contract (ARENA-02): all six window.civCamera methods present after mount; the four existing methods unchanged.
- No `bindings.ts` change: `git diff --name-only` does not list `tauri-app/src/bindings.ts` (only the two hand-written typedefs change).
- Manual UAT (GPU-only, phase gate): `npm run tauri dev`, 3-civ world — default view frames all colonies; clicking a leaderboard row focuses that civ; frame-all resets; interaction stays smooth (no frame collapse).
</verification>

<success_criteria>
- The camera frames all living colonies by default and re-frames on civ collapse (REN-02).
- window.civCamera.focusCiv(civId) focuses one civ and frameAll() resets — added additively with the existing four methods intact (ARENA-02).
- Selecting a civ (selectedCivId / leaderboard row click) drives focusCiv; clearing it calls frameAll.
- Pure camera helpers are named exports unit-tested under vi.mock("phaser"); the additive bridge contract is component-tested; tsc 0 and full suite green; no bindings regen.
</success_criteria>

<output>
After completion, create `.planning/phases/02-w8-renderer-multi-civ-identity/02-02-SUMMARY.md`
</output>
