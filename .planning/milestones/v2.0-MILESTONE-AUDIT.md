---
milestone: v2.0
milestone_name: Civ Simulation
audited: 2026-06-07
status: passed
scores:
  requirements: 17/17
  phases: 5/5
  integration: clean
  flows: complete
nyquist:
  compliant_phases: [1, 2, 3, 4, 5]
  partial_phases: []
  missing_phases: []
  overall: COMPLIANT
gaps: {}
tech_debt:
  - phase: 02-w8-renderer-multi-civ-identity
    items:
      - "SC#3 chunked-terrain RenderTexture perf rewrite — accepted re-scope (civs capped at 3); backlog perf optimization, not needed at milestone scale."
      - "MED-04: real-scene installCameraApi test + missed-focus-on-scene-ready retry — deferred (needs a civCameraReady signal; would be a production restructure)."
  - phase: 04-w6-combat-diplomacy
    items:
      - "INFO: civ_strength reads the population mirror mid multi-attack pass (self-heals at resolve_environment); a dead (alive==false) civ can still be raided next turn — design edges, not defects."
  - phase: 05-w5-genetics-depth-selection
    items:
      - "INFO: gene_mortality_modifier NaN-temp absorption doc comment credits clamp vs max (result still finite); PATTERNS/COMMON_PATTERNS are duplicate consts."
  - cross-phase
    items:
      - "WARNING-1 (GEN-01/ARENA-01): text-state exposes the expressed phenotype (morph+pattern) but NOT raw gene values (strength/resistances/alleles) — intentional phenotype-visible/genotype-hidden design; confirm this is the intended harness visibility boundary."
      - "WARNING-2 (ENV-02): plague/landslide effects surface via environment.disasters (exposed) not the modifiers list — harness authors must read disasters, not only modifiers."
human_verification_pending:
  - "Manual UAT of the live multi-civ sim (every phase has Manual-Only VALIDATION items) — needs real providers (~/.xolotl-code/config.json keys) + the WebView2 desktop UI; cannot run on this Windows host autonomously."
  - "CI run of the backend cargo tests (Linux/macOS) — backend #[test]s compile here (cargo test --no-run exit 0) but execute on CI per CLAUDE.md gotcha #5."
---

# Milestone v2.0 "Civ Simulation" — Audit

**Verdict: PASSED.** All 17 requirements satisfied in code, all 5 phases verified + Nyquist-signed-off, cross-phase integration clean, the E2E sim+harness flow holds together. No critical gaps. Remaining items are (a) minor tech-debt/design-confirmation notes and (b) human/CI verification of live behavior that cannot run on a Windows host.

## Definition of Done

> Milestone goal (ROADMAP): turn the Axolotl Civilization game into a living, watchable,
> multi-AI-civilization simulation — and a harness eval/arena where agentic harnesses
> compete on a shared scoreboard.

Achieved in code: a user can create a 1-3 model civ world; it renders with per-civ identity and a multi-colony camera; the turn loop drives a living environment (seasons, disasters, resources), multi-civ decisions, territory/combat/diplomacy, wild predators, and evolving genetics; a leaderboard ranks living civs; and an external harness reads the full state as text and drives a civ on a controller-attributed scoreboard.

## Phase Verification Summary

| Phase | Name | Verify | Nyquist | Code Review |
|-------|------|--------|---------|-------------|
| 01 | W9-lite Multi-Model World Creation + Leaderboard | PASS (6/6) | ✅ | (recovered + verified) |
| 02 | W8 Renderer Multi-Civ Identity | PASS (logic; manual GPU UAT) | ✅ | 1 HIGH + 4 MED fixed |
| 03 | W4 Environment Engine | PASS (logic; CI/manual) | ✅ | 1 MED fixed (cosmetic disasters → real effects) |
| 04 | W6 Combat & Diplomacy | PASS (4/4) | ✅ | CLEAN |
| 05 | W5 Genetics Depth & Selection | PASS (2/2) | ✅ | CLEAN |

## Requirements Coverage (17/17 satisfied)

3-source cross-reference (phase VERIFICATION.md "passed" + SUMMARY `requirements-completed` + REQUIREMENTS.md traceability) + the integration map all agree:

| Group | Requirements | Status |
|-------|--------------|--------|
| Civ core | CIV-01, CIV-02, CIV-03 | satisfied (Phase 1) |
| Harness arena | ARENA-01, ARENA-02, ARENA-03 | satisfied (Phase 1, preserved through 2-5) |
| Renderer identity | REN-01, REN-02 | satisfied (Phase 2) |
| Environment | ENV-01, ENV-02, ENV-03 | satisfied (Phase 3) |
| Combat & diplomacy | WAR-01, WAR-02, WAR-03, WAR-04 | satisfied (Phase 4) |
| Genetics & selection | GEN-01, GEN-02 | satisfied (Phase 5) |

No orphaned requirements; no unsatisfied requirements. (GEN-01/ARENA-01 satisfied at the phenotype level — see WARNING-1.)

## Cross-Phase Integration — CLEAN

`gsd-integration-checker` verified every seam WIRED end-to-end:
- **Turn-loop order:** TurnStarted → `tick_environment` (P3, fires predator_incursion[P4]/plague[P5]) → per-civ decision loop (P1 + P4 claim/diplomacy/trade; attacks queued) → `resolve_combat` (P4) → `step_predators` (P4) → per-civ `resolve_environment`→`run_life_cycle` (P3 env + P5 selection + breeding). Coherent.
- **Population-mirror invariant holds** across all three entity-removing passes (combat, predators, selection) — entities removed, never the counter; mirror re-syncs at end of run_life_cycle after combat+predators.
- **Data seams closed:** P3 predator_incursion→P4 spawn_predators; P5 plague forecast/fire→P5 mortality; P4 civ_strength←P5 genes.strength; P5 phenotype→text-state; P2 selectedCivId→focusCiv; P1 controller→leaderboard.
- **ARENA contract intact (ARENA-02):** render_game_to_text keeps legacy keys + additive civs[]/leaderboard/environment; codex-play-civ.mjs still parses; civPilotControls.start legacy shape preserved.

## Live Gates (re-run at audit)

- `cargo test --no-run` (tauri-app/src-tauri) → exit 0
- `cargo clippy --all-features -- -D warnings` → 16 pre-existing baseline errors only, ZERO new across all 5 phases
- `npx tsc --noEmit` (tauri-app) → exit 0
- `npm test` (vitest) → 245 passed / 26 files

## Nyquist Compliance — COMPLIANT (5/5)

All five phases have `nyquist_compliant: true` VALIDATION.md with populated per-task maps and signed-off checklists. Backend tests compile on Windows and run on CI; frontend tests run locally.

## Tech Debt & Notes (non-blocking)

See frontmatter `tech_debt`. Highlights: SC#3 chunked-terrain perf (backlog re-scope), MED-04 real-scene camera test (deferred), and the two cross-phase design-confirmation WARNINGs. None block the milestone.

## Human / CI Verification Pending

See frontmatter `human_verification_pending`. The milestone is implemented + verified in code; what remains before declaring it shipped is: (1) manual UAT of the live multi-civ sim (needs providers + WebView2), and (2) the CI run of the backend cargo tests. Neither can run on this Windows host.

## Recommendation

Milestone v2.0 is **code-complete and audit-passed**. Proceed to `/gsd-complete-milestone v2.0` (archive + tag) once the human/CI verification above is satisfied — or accept the documented tech debt and complete now, with manual UAT tracked as a follow-up.
