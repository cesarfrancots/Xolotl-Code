name: Rust CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

env:
  CARGO_TERM_COLOR: always
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    name: Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Pinned Rust version for reproducible CI. Bump intentionally after local testing.
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.95.0
        with:
          components: rustfmt, clippy

      # macos-latest sometimes ships a pre-existing Homebrew rustup whose
      # `cargo` shim fails with "unexpected argument 'fmt'" when a second
      # toolchain is installed. Force the dtolnay-installed toolchain as the
      # default so PATH resolves correctly across all runners.
      - name: Pin default toolchain
        shell: bash
        run: |
          rustup default 1.95.0
          rustc --version
          cargo --version

      - name: Restore cache
        uses: Swatinem/rust-cache@v2
        with:
          shared-key: ${{ matrix.os }}-cargo-cache
          workspaces: ./rust -> target

      - name: Check formatting
        working-directory: rust
        run: cargo fmt --all -- --check

      - name: Clippy lints
        working-directory: rust
        run: cargo clippy --workspace --all-features -- -D warnings

      - name: Build
        working-directory: rust
        run: cargo build --workspace --verbose

      - name: Run tests (excluding upstream-dependent tests)
        working-directory: rust
        run: cargo test --workspace --exclude compat-harness --verbose

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Pinned Rust version for reproducible CI. Bump intentionally after local testing.
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@1.95.0
        with:
          components: rustfmt, clippy, llvm-tools-preview

      - name: Pin default toolchain
        run: rustup default 1.95.0

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-llvm-cov

      - name: Generate coverage
        working-directory: rust
        run: cargo llvm-cov --workspace --exclude compat-harness --lcov --output-path lcov.info

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: rust/lcov.info
          fail_ci_if_error: false
